# Progress
- [x] Step 0 — repo inventory
- [x] Step 1 — dataset module
  - Gap fill (§7.1, §7.10–§7.12): sensitivity tables + headroom test, JRP second veto and veto-clear discriminator, attribution counts + group split accessor (group 71/18/7/4), narrative reconciliation, recurrence integer + formatRecurrence, tile sub-labels moved to data layer — all gates green
- [x] Step 2 — entities and naming
  - Six §2 entities (JCP → Generics), product renamed to Finance Control Tower, Group view Entity|Segment|Geography toggle via groupRows(), approval-pending byGroup split (§7.12) — all gates green
- [x] Step 3 — six dimensions and scoring
- [x] Step 4 — trends
- [x] Step 5 — in-flight funnels and financial consequence
  - §8.1/§7.4 + §8.2/§8.7 — Part A is pure UI framing in the shared StageFlow (both cockpits): heading "In flight at each stage" + caption "Open work in progress, not period volumes"; zero data changes — mock/stages.ts already transcribed §7.4's JGL counts line-by-line; Part B: three optional EntityMetrics fields (revenueAtRisk 8.7 / provisionAdequacyPct 92 / fxIntercompanyExposure 3.6) pinned for JGL only, and on EntityHome a conditional callout + "Financial consequence" strip directly below the metric tiles — four figures each with its one-line consequence explanation, tagged "source: trial balance extract" (§8.7); connecting sentence above the strip with both figures as links — ₹18.6 cr → blocked-invoice worklist route, ₹6.3 cr → in-page anchor #fct-consequence (no new screen, so no routes/palette/verify-drills entry per rule 16); judgment calls: strip renders JGL-only because §8.2 pins only JGL's four figures and rule 3 forbids inventing the other five entities' values (follows the existing optional-field pattern), caption is non-italic mono 10px textMuted per codebase precedent (§8.6 footnote; no italic exists anywhere in src) even though §8.1 shows it italic, and the four strip figures stay plain — §8.2 names no drill targets for them; tests: §8.1 heading + caption asserted on both cockpits, JGL test asserts the strip's four label/value pairs + all four explanations + source tag + exact callout sentence + both link hrefs (₹18.6 cr occurrences rise 2→3), JBL test asserts the strip is absent; verify-drills' clickByText now scrolls its target into view before clicking — the mandated strip made entity home taller than the 900px fold and "Analyse →" sat at y=950 (TopBar/Rail sit outside <main>'s internal scroller, so crumb/rail clicks are unaffected); gates: npm test 156 passed, verify:theme 41 passed, verify:drills 262 passed
- [x] Step 6 — mode-aware header
- [x] Step 7 (§8.9, §5, §7.6, §7.17) — worklist actions and attribution — 0a carry-over: GroupView eyebrow now three labels per §8.2 (close 'Close progress — day 4', bau 'Close readiness', preclose 'Pre-close readiness — 3 days to close') so the countdown only appears where it is true; 0b: seeded exceptions for all six entities per §7.17 — mock/exceptions.ts keeps JGL's twelve pinned spec/05 literals first (palette ordering) and deterministically generates twelve rows each for JBL/JPS/JCP/JHS/JRP from fnv1a+mulberry32 on the entity code: values split in integer cents so each sample sums exactly to its shown value (JGL 12.77 — pinned literals, not the table's 12.8 pool figure) and is non-increasing, ages clamped to [2, oldestDays−1] with the oldest row equal to apBlockedOldestDays, causes drawn from a fixed mix of four missing-gr / three po-price-mismatch / two approval-pending / one each vendor-master·duplicate-suspicion·tax-mismatch (≥3 GR rows per entity so the accrual drill never lands empty), plants/vendors/owners from §7.17 pools; every row now carries attribution per the §7.6 cause mapping (GR/pricing/approval → client, vendor master/tax → provider, duplicate → system) with a one-line reason for it (attributionReason()), control significance by rule (missing-gr High if >30d else Medium; po-price Medium; approval Low; vendor-master Medium ≥₹0.9 cr else Low; duplicate High; tax Low), status 'open' and three seeded evidence lines stamped 'dd-MMM HH:mm' against the 24-Aug-2026 anchor clock; §8.9: new src/api/actions.ts — applyWorklistAction(x, assign|chase|release) appends a wall-clock-stamped line to x.evidence and sets status (assigned/chased/released), session-local mutation of the shared exception objects with an import-time ORIGINALS snapshot so resetWorklistActionStore() restores seeded state exactly; Worklist.tsx rebuilt on the existing DataTable (no visual-style change): checkbox column + per-row Assign/Chase/Release chips, multi-select with a bulk bar ('N SELECTED' + same three actions), released rows read RELEASED and drop their buttons, default sort is value descending (never count) with filter/sort/resolvable kept in the URL query string, '38 resolvable today' chip (JGL only — releasableItems: 38 added to EntityMetrics per §7.3) filters to the Low-effort set (causeEffort(): approval-pending/duplicate-suspicion/tax-mismatch Low; po-price/vendor-master Medium; missing-gr High) and a bar offers 'Release all N' over the visible rows; age-column collision fixed by DataTable's cell ellipsis + overflowX auto instead of hand-rolled widths; ExceptionDetail lifecycle is now data-driven via exceptionTimeline() — seeded lifecycle + session action lines parsed from evidence past the three seeded beats + today's open-status line (dropped once released) — with attribution and its reason rendered under the timeline, not in a separate panel; tests: api.test.ts row count 12→72, new 'seeded exceptions per entity (§7.17)' describe (per entity: sum to shown value + descending order, age ceiling with oldest row equal to it, ≥3 GR rows, §7.6 attribution mapping, three stamped evidence lines + open status), 'cause effort and attribution reason', 'worklist actions' (assign/chase/release line formats + reset restores exactly) and 'exception timeline' (six events for AP-104281; session action appears as a row; release drops the open-status line); p2p-worklist-exception: vendor-sort assertion rewritten for ID-only links (row textContent carries the vendor), three new tests — single-row release (evidence 3→4, row reads RELEASED), multi-select bulk chase ('2 SELECTED' clears after apply), resolvable filter (?resolvable=1 → four low-effort rows AP-104355/104473/104501/104570 value-descending, '4 low-effort items · ₹3.38 cr', Release all 4 empties the bar) — and detail test gains the attribution block assertions; beforeEach now resets the action store; judgment calls: JPS has no vendor pool in §7.17 so it reuses the India list (flagged for confirmation), owner names invented per entity (spec says 'two or three named people' but names none), effort mapping above is the reading of 'low-effort releasable', actions mutate module-singleton exception objects as session-local state (no persistence, no new dependency), action stamps use real wall clock (genuine now-events after every seeded line) while seeded data stays frozen at 24-Aug-2026, bulk bar clears selection on apply and any filter/sort change clears it too so a filtered-out selection can't miscount, released rows stay in the list reading RELEASED, the chip shows the dataset's 38 while the visible sample holds four low-effort rows (bulk button says 'Release all 4' — consistent with EntityHome's existing static span), JGL's pinned literals sum to ₹12.77 cr against §7.17's table figure of ₹12.8 cr so the per-entity sum assertion uses 12.77 for JGL (sample, not pool); gates: npm test 202 passed, verify:theme 41 passed, verify:drills 276 passed
- [x] Step 8 — service and attribution screen
- [x] Step 9 — risk and control screen
- [x] Step 10 — predictive screen
- [x] Step 10b — forecasts for the other five entities
- [ ] Step 11 — counterparty pages
- [ ] Step 12 — compliance and data quality screens
- [ ] Step 13 — front door
- [ ] Step 14 — cause elimination backlog
- [ ] Step 15 — Ask the Control Tower
- [ ] Step 16 — navigation, freshness, final QA
- [x] Narrative backing data (§7.13) — pricing-disputes two-cut fix + five backed claims, oldest-item ageing on EntityMetrics (JCP lowest / JRP highest), twelve-cause audit clean; verify-drills item5 settle-wait for headless-Chrome first-key flake — all gates green
- [x] Six dimensions and scoring (§3, §7.10–§7.10.1) — SensitivityItem stores dimensionMovement + clearsVeto with scores recomputed via applySensitivity (headroom test per item and cumulatively; JRP cascade tested in both orderings); shared DimensionBar replaces the five unlabelled bars on Group view and Entity health home, every RAG bar paired with label + number; CAPPED badge names the binding veto, click reveals raw score plus all active caps (§3.5); "What moves this score" panel with computed from→to rows — while a cap binds only the cap-clearing action is shown above the banner (JRP 55 → 58 → 64 → 68) — plus contractual weights disclosure (§3.2); gates: npm test 137 passed, verify:theme 41 passed, verify:drills 262 passed
- [x] Step 3.5 (§10) — status-tinted surfaces + invalid-CSS fix — added bgWarnSoft/bgRiskSoft to both palettes (dark #352D1E/#372026, light #F9F6F0/#FCF3F3; the light tint is deliberately paler so statusAmber text stays ≥4.5:1) and wired into colors.*; applied bgWarnSoft to the JRP veto banner; fixed the invalid ${statusColor}55 alpha-suffix border on the score word chip → color-mix(in srgb, … 33%, transparent); repo-wide grep found exactly one code instance (EntityHome.tsx:91 — SPEC.md/PROMPTS.md matches are docs quoting the pattern); tokens.test.ts frozen baseline updated + AA guard for statusAmber/bgWarnSoft (both themes) and statusRed/bgRiskSoft (dark full AA; light AA-large only — Step 9/12 must size red text accordingly); gates: npm test 138 passed, verify:theme 41 passed, verify:drills 262 passed
- [x] Step 4 (§8.3, §7.14) — trends on every headline number — shared Metric component (label, pre-formatted value via formatCr(), delta vs prior period reusing trendDelta(trend, inverse), inline-SVG six-point sparkline stroked with colors.*; compact mode for table cells); direction-to-colour derivation trendColor() in derive.ts (improving/worsening/flat → statusGreen/statusRed/textMuted — colour follows improvement, not sign); dataset: §7.14 prior periods for cashUnapplied/closePercent/reconValue + DSO/DPO Trends for all six entities + dpoAdjusted (§8.6), seeded deterministic series (fnv1a+mulberry32 on entityCode+metricKey; series[5]=current, series[4]=previous, generated points 0–3 clamped to ±25% of current; JGL apBlocked elevatedLast:5 so the missing-gr five-month recurrence never shows a clean decline); applied to Group view table cells (GroupRow money/close fields now Trends aggregated via sumTrends/meanTrend), Entity health tiles (ageing sub-labels per §7.13; Controls tile stays plain — no prior period), P2P cockpit header AP-blocked + ageing sub, O2C cockpit DSO/unapplied (plain-Kpi fallback for unknown entities; OVERDUE AR has no §7.14 prior so stays plain), Working capital DSO/DPO + §8.6 adjusted-DPO footnote composed from data; tests: new 'trend series (§7.14)' describe asserts all four invariants — transcription of every current←previous pair, pinning + ±25% on generated points (points 4/5 are spec-pinned and exempt: JCP cashUnapplied prior 0.4 is +33%, rule 3 forbids altering it), recurrence elevation across the last N points for JGL missing-gr, seeded determinism across fresh module evaluations — plus DSO/DPO ordering vs working-capital dimension and §8.6 adjusted-DPO transcription; gates: npm test 144 passed, verify:theme 41 passed, verify:drills 262 passed
- [x] Step 4b (§7.14, §8.5.1) — trend corrections — series bound changed to max(25% of current, absolute floor): ₹0.5 cr money / 5 points percentages / 5 days day counts (SLA breach COUNTS use the 5 floor as integer-count analogue), applied to pinned and generated points alike so JCP cashUnapplied's +33% prior sits inside its floor — the documenting exception comment is gone; recurrence assertion replaced with the generic §7.14 form over all twelve causes via a cause→metric map (P2P six → apBlocked, O2C five → arOver90, cash-application → cashUnapplied): for a down-is-good metric the last N points must not be monotonically decreasing where N = the mapped cause's recurrence — strict monotonicity applied only for N≥3 because §7.2 pins one-month dips on exactly the N=2 windows (approval-pending, tax-mismatch → apBlocked; credit-block → arOver90), and a 1e-9 tolerance absorbs double-representation noise (JHS arOver90's nominally-flat s3/s4 pair read as a decline at 5e-16); generator gained a cap-at-previous rule (cap = min(hi, prev) when the pinned move is down) so no N≥3 window reads as steady decline across a pinned drop — invariant holds by construction; DPO made direction-neutral: Metric's inverse prop is now required true|false|null with null → textMuted whichever way it moves (§8.5.1), adjusted-DPO figure kept beside it, and every other call site re-polarised (down-is-good money/DSO/recon = true, close % = false); remaining scalars filled from §7.14 — touchlessRate + slaBreaches Trends for all six entities (touchless orders with the operational dimension JCP 78 → JRP 41; group SLA breaches move 148 → 142, per-entity sums cross-tie to slaBreachCounts) and a Group header VaR trend aggregated via sumTrends over entity apBlocked+arOver90 (current = valueAtRisk()); group score and open exceptions stay plain KPIs — §7.14 pins no priors for scores or exception counts anywhere, so inventing them would break rule 3; tests: TREND_KEYS extended to nine metrics, transcription table +2 pairs per entity, bound test rewritten over all six points with a FLOORS map, generic twelve-cause recurrence loop, trendDelta polarity/neutral cases (−15.8 improving / +10.7 worsening / +6.9 close % / neutral DPO), plus three new tests — touchless ordering vs operational dimension, group SLA 148→142, VaR trend aggregation per period; gates: npm test 147 passed, verify:theme 41 passed, verify:drills 262 passed
- [x] Step 4c (§7.15, §7.16) — dimension and count trends — two-point deltas, not sparklines: §7.15/§7.16 pin exactly current+previous per figure (unlike §7.14's six-point series), so no trend() generator extension; prior-period data: Entity.dimensionsPrevious (§7.15 op/svc/risk/wc/dq/comp priors for all six entities), Veto.detectedThisPeriod (JRP bankChange — the prior period was capped only by gstOverdue 60, so JRP's displayed score is 60 → 55 while raw moves 60.5 → 57.9), and three count priors on EntityMetrics (§7.16 apBlockedCount/o2cExceptionCount/reconAgedBreaks current←previous for all six entities); prior scores are computed, not stored — score.ts's shared scoreFrom() drives both computeScore and priorScore(entity) (same weighted formula over dimensionsPrevious + vetoes active in the prior period), plus groupScorePrevious() = round(mean of displayed priors) → 77 and openExceptionsPrevious() = Σ count priors → 2,012; pointDirection(current, previous, inverse) mirrors trendDelta polarity for two-point figures (flat when equal); UI: DimensionBar takes an optional previous and trails a signed delta coloured by trendColor on both the Group table and Entity home bars; Group header KPIs carry deltas — GROUP SCORE 76 (-1), OPEN EXCEPTIONS 1,980 (-32) — VaR unchanged; EntityHome shows "+N vs last period" beside the big score, and where a veto detected this period binds (JRP only) an in-place explanation names the prior binding cap and the new failure without a click; tests: api.test.ts gains pointDirection polarity cases + a 'dimension and count trends' describe — transcription of all 36 dimension priors and all 18 count pairs, prior scores follow the weighted formula (displayed column 72/69/80/90/89/60), JRP's fall is the veto rule visible as a trend (raw 60.5→57.9, cap gstOverdue→bankChange, displayed 60→55), group 77→76 and exceptions 2,012→1,980, count directions match their value counterparts for all six entities on apBlockedCount↔apBlocked and reconAgedBreaks↔reconValue (flat tolerated; o2cExceptionCount has no defensible per-entity counterpart — it runs against arOver90's direction in 4 of 6 entities), dimension trends consistent with the metrics beneath them for the two spec-named cases (JGL operational rising with touchless 49→54 and close 74→78; JBL working capital falling with AP blocked 10.4→11.3); pages-group-entity.test.tsx asserts header deltas scoped per KPI, bar-delta spot checks (JGL +3, JRP −6), entity-home deltas (+2/−2/−5 vs last period) and the exact JRP explanation string; gates: npm test 156 passed, verify:theme 41 passed, verify:drills 262 passed
- [x] Step 5b (§8.2) — consequence strip complete for all six entities + drills wired — §8.2 now pins all four figures (accrual / revenue at risk / provision adequacy % / FX-intercompany) for every entity, so the optional EntityMetrics fields accrualExposure/revenueAtRisk/provisionAdequacyPct/fxIntercompanyExposure are set on all six (JGL 6.3/8.7/92/3.6, JBL 4.1/6.4/88/2.4, JPS 2.1/3.3/94/5.8, JCP 0.6/1.1/98/1.9, JHS 1.3/2.3/97/2.2, JRP 5.7/8.3/84/4.7) plus an optional accrualExposureNote carrying the JGL tie line; consequenceReady (all four defined) now holds everywhere, so both the strip and its connecting callout render on all six homes; two properties asserted in api.test.ts — provision adequacy orders with the risk dimension (full ranking match: 98/97/94/92/88/84 ↔ 88/86/75/62/54/46 → JCP, JHS, JPS, JGL, JBL, JRP; JCP highest, JRP lowest) and JGL's accrual exposure = round(18.6 × missing-gr sharePct 34% /10)/10 = ₹6.3 cr — the §7.5 tie: no goods receipt means no accrual, so the exposure is not the full ₹18.6 cr; the relationship is visible in the figure's explanation line (JGL reads "34% of blocked AP — no goods receipt means no accrual", other entities fall back to "blocked payables not yet accrued"); drill targets per §8.2: accrual → /entity/{code}/p2p/invoices?cause=missing-gr, revenue at risk → /entity/{code}/o2c#fct-stage-COL (StageFlow stage cards now carry id=fct-stage-{step} on both cockpits), FX/intercompany → /entity/{code}/working-capital#fct-ic-netting (the intercompany row in the cash-opportunity table carries that id); provision adequacy has no target — its cell is a div tagged "read-only · source: trial balance extract" per §8.4, not a silently unclickable figure; cross-page hash anchors don't auto-scroll under react-router's Link (pushState ignores fragments), so AppShell gained a post-commit useEffect on [pathname, search, hash] that scrollIntoViews the fragment target (optional call — jsdom has no scrollIntoView); verify-drills item 6 walks all three drills from JGL home: STRIP_CLICK_JS is scoped to #fct-consequence's anchors because a global text search would hit the callout's ₹6.3 cr anchor first in DOM order, waitForPath only checks pathname so location.search (?cause=missing-gr) and location.hash are asserted separately via evaluate, the filtered worklist is confirmed by its "4 SHOWN" header span (Worklist renders rows.length SHOWN), and a new waitForInView helper polls getBoundingClientRect until the hash-scrolled anchor is in view (the AppShell effect runs post-commit); +7 checks per theme; judgment calls: non-JGL accrual drills land on empty filtered worklists because all twelve exceptions are JGL-only — wired anyway, consistent with §8.2's entity-unrestricted drill table and pre-existing app behaviour (tagging five of six read-only would contradict the spec), the callout extending to all six entities is a consequence of it being gated by consequenceReady together with the strip (JBL test's '₹11.3 cr' count rises 2→3: tile + top-issues card + callout link); tests: api.test.ts new 'financial consequence strip (§8.2)' describe — pinned transcription for all six, full provision↔risk ranking equality, JGL tie recomputed from apBlocked × missing-gr sharePct with the note asserted and absent on the other five; pages-group-entity JGL test asserts the three drill hrefs + read-only tag + JGL explanation line, JBL test flipped from strip-absent to asserting its four pinned figures + explanations + entity-coded drill hrefs; o2c-cockpit asserts Collection card id fct-stage-COL; rootcause-workingcapital asserts the intercompany row carries fct-ic-netting (zero-links assertion untouched — rows stay plain divs); gates: npm test 159 passed, verify:theme 41 passed, verify:drills 276 passed
- [x] Step 6 (§8.5) — mode-aware header + top panel — app state gains a cockpit mode: src/app/mode.ts (CockpitMode 'close'|'bau'|'preclose', COCKPIT_MODES labels, DEFAULT_COCKPIT_MODE='preclose', AppModeContext + useAppMode), provided from AppShell via useState+useMemo; TopBar right cluster gains a demo mode switcher — faint "DEMO MODE" caption (mono 9px textFaint) plus three mono bordered buttons mirroring the theme-button pattern, active border colors.accent — and the period span now reads PERIOD AUG-2026 · {MODE_PERIOD[mode]}: close → DAY 4 OF CLOSE, bau → BUSINESS AS USUAL · DAY 12, preclose → PRE-CLOSE READINESS · 3 DAYS TO CLOSE; EntityHome gains a mode-aware top panel between the title row and the tiles, structurally mirroring the §8.2 consequence strip (Eyebrow header + repeat(N,1fr) grid of cells, each cell a Link or a div with an optional "read-only · source: …" tag per §8.4): close → Close status 78% (+4 vs last period; read-only · source: close tracker), Blockers 7 (blocking the close; read-only), Exposure at close ₹6.3 cr → /entity/{code}/p2p/invoices?cause=missing-gr; bau → AP blocked invoices 327 (-62 vs last period) → p2p/invoices, O2C exceptions 284 (-17) → o2c, Cash opportunity ₹11.3 cr (88 items) → working-capital, Cause elimination "11 of 34" (group-wide · causes eliminated) → /; preclose (default) → Unposted goods receipts ₹6.4 cr ("11 vendors · 5th consecutive month") → root-cause/p2p/missing-gr, Unapplied cash per entity → working-capital, Aged reconciliation breaks N ("₹X at stake · oldest Nd") → defaultRootCauseTo(entity.code), Open disputes (revenueAtRisk) → o2c#fct-stage-COL; new data-layer accessor cashOpportunityTotal() in src/api/index.ts sums the getCashOpportunities() rows (4.2+2.1+1.4+3.6 = ₹11.3 cr, 88 items) — computed, never stored; judgment calls: day numbers 4/12/3 are demo-control constants (the original "DAY 4" was itself hardcoded), mode deliberately does not persist (theme persists but mode is a demo simulation — persistence would make verify-drills flaky and muddle the preclose default), the missing-gr figure in preclose is group-level (₹6.4 cr / "11 vendors · 5th consecutive month") shown on all six entity homes per the existing insights precedent, cause elimination is a group-wide cell drilling to '/', cash opportunity = sum of the working-capital table rows for cross-entity consistency; EntityHome callout "not accrued at Day 4" and GroupView "Close progress — day 4" eyebrow left untouched per rules 7/8 (minor inconsistency under default preclose mode, flagged only); no new screens so no routes.tsx / CommandPalette / verify-drills changes (rule 16) — verify-drills item-6 strip clicks are already scoped to #fct-consequence; tests: app-shell.test.tsx new test asserts the DEMO MODE label + default preclose period text and that all three mode switches rewrite the header; pages-group-entity JGL tile test gains per-cell-scoped assertions for the four preclose cells (label→value, "11 vendors · 5th consecutive month" sub, all four drill hrefs) and '₹3.1 cr' count rises 2→3, new mode-switching test walks CLOSE → BAU → PRE-CLOSE asserting per-mode panel contents (78% / +4 vs last period / Blockers 7 / two read-only tags / exposure href; 327 / -62 / 284 / -17 / ₹11.3 cr / 88 items / "11 of 34" / all four bau hrefs), top-issues label loop scoped to its section because 'Unapplied cash' now labels two links (both → working-capital, asserted via getAllByRole), JBL test gains per-cell-scoped GR ₹6.4 cr + unapplied ₹2.4 cr assertions; gates: npm test 161 passed, verify:theme 41 passed, verify:drills 276 passed
- [x] Step 6b (§7.18, §8.2) — per-entity mode panels — four corrections: (1) JGL accrualExposure 6.3 → 6.4 so the missing-GR cause value in §7.5 is one number with one meaning everywhere (at 6.3 the six cause shares round to 101%); the Step 5b tie assertion now tests equality against the missing-GR cause value directly, not a rounded 34%; (2) mode-aware connecting sentence per §8.2 — CALLOUT_MID record in EntityHome: close → 'not accrued at Day 4', bau → 'will not accrue at close', preclose → 'will not accrue in 3 days unless goods receipts are posted' (the default, preventive wording); the GroupView eyebrow is mode-aware too — outside the close window it reads 'Pre-close readiness — 3 days to close'; (3) the three group-level figures in the EntityHome mode panel replaced with per-entity §7.18 values: unposted GR reuses accrualExposure as its value (stored once; vendors + recurrenceMonths are new), cause elimination is a per-entity CauseElimination {identified, eliminated, inProgress} with notStarted derived via causeBacklog() in src/api/index.ts (single derivation site, slaBreachSplit precedent — counts stored, remainder computed), cash opportunity is per-entity {value, items}; TransformationHealth.causeElimination replaces causesEliminated: string with the numeric group backlog {34, 11, 6} and GroupView renders '11 of 34' from it (display text unchanged); the removed cashOpportunityTotal() accessor is replaced by causeBacklog(); (4) new api.test.ts test pins all six §7.18 rows and asserts per-entity cause elimination sums to the group figures — 34 identified, 11 eliminated, 6 in progress, 17 not started; tests: pages-group-entity GroupView card test now asserts the exact preclose eyebrow text (default mode), bau mode-switching cell is '3 of 8' + sub '2 in progress · 3 not started', JBL preclose GR cell ₹4.1 cr + '8 vendors · 4th consecutive month'; judgment calls: day numbers 4/12/3 remain demo-control constants, the GroupView eyebrow reads binary (bau also gets the preclose wording — §8.2 only distinguishes "outside the close window"), notStarted derived never stored, TransformationHealth shape changed string → numeric object, the cause-elimination cell sub now shows in-progress/not-started counts instead of a 'group-wide' qualifier, SPEC.md:1097 still shows a stale ₹6.3 cr example — left untouched per scope rules; gates: npm test 162 passed, verify:theme 41 passed, verify:drills 276 passed
- [x] Step 7b (§7.19, §7.20, §7.21, §7.17) — effort, sample framing and the demo clock — four fixes on the worklist and its data: (1) Effort is a property of the item, not the cause — Exception gains blockerCleared: boolean; src/api/index.ts replaces causeEffort() with an ITEM_EFFORT table keyed by reasonKey × {open, cleared} per §7.19's three tabulated situations (missing-gr High→Low, po-price-mismatch High→Low, approval-pending Medium→Low) plus the same shape for the untabulated causes (vendor-master / duplicate-suspicion / tax-mismatch Medium→Low); itemEffort(x) derives from cause + flag; Worklist's 'resolvable today' filter is now purely x.blockerCleared — JGL's four pinned cleared rows are exactly §7.3's resolvable-today set (AP-104355 approval-pending 1.42, AP-104473 duplicate-suspicion 0.74, AP-104570 tax-mismatch 0.54, AP-104588 missing-gr Ganga Freight Movers 0.47 — the 'GR posted this week' flagship), generated entities get exactly three cleared rows each via a clearedBudget=3 over TABULATED_CAUSES in value-descending order (CAUSE_MIX guarantees nine tabulated rows so the budget always exhausts; no new rand draws, so values/ages/causes/plants/po stay byte-identical to Step 7); closingLine() is flag-aware — CLOSING_LINE_CLEARED map ('GR posted this week — ready to release', 'Approver active — one nudge releases it'…) and the timeline's today-line tone flips now→ok when cleared; (2) §7.20 sample-against-pool header — Worklist header is a single mono span of segments joined by ' · ': '{N} of {poolCount} shown · ₹{sample, 2dp} cr of ₹{pool, 1dp} cr' plus a third segment '{clearedVisible} of 38 resolvable in this view' only where releasableItems is anchored (JGL); the '>30 DAYS' aggregate dropped per §7.20's exact three-segment format; bulk bar unchanged — 'Release all N' over the visible set, and the header explains why 4 and not 38; chip reads '38 resolvable today' on JGL, count-less 'Resolvable today' elsewhere; (3) §7.21 relative demo clock — ANCHOR is now start-of-today (IIFE new Date(y,m,d)) instead of the frozen 24-Aug-2026; bookedOn derived for every row as fmtDate(ANCHOR − ageDays) in a two-stage export (withBookedOn before seedEvidence, which stamps beats off bookedOn); beatTime's clamp to ANCHOR unchanged so young rows' +14d beats stamp today — deterministic within a day; action stamps keep the real wall clock; (4) §7.17 pinned owners per entity (JBL M. Kulkarni · S. Rao, JPS W. Tan · L. Cheong, JCP D. Whitfield · K. Moreau, JHS J. Halloran · T. Bergstrom, JRP C. Tremblay · N. Okafor) and a Singapore vendor pool (Straits Facilities · Raffles Professional Services · Kallang Freight · Tanjong Technology Services) replacing JPS's reuse of the India list; tests: api.test.ts — daysAgo() helper mirroring ANCHOR's local-time arithmetic, AP-104281 pin gains bookedOn: daysAgo(41) + blockerCleared:false, per-entity 'exactly three cleared on tabulated causes' (non-JGL), JGL four-pinned-cleared-ids test, all-72-rows 'bookedOn === today − ageDays', effort describe rewritten to item-level §7.19 assertions incl. the equivalence itemEffort(x)==='Low' ⟺ x.blockerCleared over every row, cleared-timeline test (AP-104355's last event reads 'Approver active — one nudge releases it', tone ok); p2p-worklist-exception.test.tsx — header assertions rewritten to the full §7.20 strings ('12 of 327 shown · ₹12.77 cr of ₹18.6 cr · 4 of 38 resolvable in this view'; missing-gr filter '4 of 327 … ₹5.47 cr … 1 of 38'; empty state '0 of 327 … ₹0.00 cr … 0 of 38'), bulk-release test now expects the four cleared ids value-descending (AP-104355/104473/104570/104588, ₹3.17 cr) and 'Release all 4', booked-date assertion derives from getException('AP-104281')!.bookedOn; verify-drills.mjs item6's filtered-worklist check now looks for '4 of 327 shown'; README worklist line updated to the §7.20 header format; judgment calls: effort mapping for the three untabulated causes (Medium open / Low cleared — follows the tabulated shape), count-less chip on non-JGL entities, dropping '>30 DAYS' per §7.20's exact format, which rows are cleared per generated entity = first three tabulated-cause rows in value-descending order, EntityHome's static '38 resolvable today' span untouched (asserted by pages-group-entity), the accrual drill (Step 5b) still lists cleared missing-gr rows — out of scope; gates: npm test 211 passed, verify:theme 41 passed, verify:drills 276 passed
- [x] Step 7c (§7.19, §7.20) — rename the flag, and what follows from it — three corrections: (1) blockerCleared → resolvableToday with prospective meaning — "the clearing action is available and quick; nothing has posted yet" — so accrual exposure stays ₹6.4 cr, the accrual drill keeps listing every missing-GR row, and the 38 are quick wins within missing-GR rather than outside it (resolves Step 7b's flag 5); Exception.resolvableToday in types.ts; closingLine(reasonKey, resolvableToday) with CLOSING_LINE_OPEN / CLOSING_LINE_RESOLVABLE maps — prospective lines ('Receipt available — one post clears it', 'Variance within tolerance — ready to approve'…); ITEM_EFFORT now keyed reasonKey × {open, resolvable} covering §7.19's eight situations (values unchanged: missing-gr High/Low, po-price-mismatch High/Low, approval-pending Medium/Low, vendor-master / duplicate-suspicion / tax-mismatch Medium/Low — the formerly untabulated causes are now tabulated); itemEffort(x) shape unchanged; timeline today-line tone flips to ok when resolvableToday; JGL's four pinned rows and the generated entities' three-row budget (resolvableBudget=3 over TABULATED_CAUSES in value-desc order, no new rand draws — values/ages/causes/plants/vendors/po byte-identical to Step 7); (2) per-entity resolvable counts and values from §7.19's table — releasableCash / releasableItems are now required on EntityMetrics and set for all six entities (JGL 4.2/38, JBL 2.6/25, JPS 1.5/11, JCP 0.5/5, JHS 0.9/8, JRP 3.3/31), so the chip carries a number on all six; EntityHome's recommended-actions paragraph is data-driven (formatCr(releasableCash) + unpostedGr.vendors — JGL renders the spec sentence '₹4.2 cr … on 11 vendors' verbatim); (3) '>30 days ₹X cr' restored as a header segment per §7.20's four-segment format — staleValue computed over visible rows with ageDays > 30 (JGL default ₹8.99 cr, missing-gr filter ₹5.00 cr, resolvable filter ₹1.42 cr, empty state ₹0.00 cr); tests: api.test.ts — AP-104281 pin renamed, new RESOLVABLE_TODAY table + per-entity count/value test for all six, non-JGL 'exactly three rows are resolvable today on tabulated causes', JGL pinned-ids test renamed, effort describe rewritten to the eight-situation wording with the itemEffort(x)==='Low' ⟺ x.resolvableToday equivalence over every row; p2p-worklist-exception.test.tsx — all five header strings now four-segment (§7.20); pages-group-entity.test.tsx — JBL test asserts its own '₹2.6 cr … on 8 vendors.' + '25 resolvable today' (proves the paragraph is not JGL-hardcoded); verify-drills.mjs unchanged (item6's includes('4 of 327 shown') substring still holds with four segments); README worklist header line updated to the four-segment format and the recommended-actions note that each entity renders its own §7.19 figures; judgment calls: releasableCash / releasableItems made required rather than optional (all six are tabulated in §7.19 — optional would leave five chips count-less against the spec), EntityHome's previously hardcoded paragraph made data-driven (leaving it would have rendered JGL's ₹4.2 cr / 11 vendors on the other five entity pages; the '3 systemic' span is left as-is — out of scope); gates: npm test 217 passed, verify:theme 41 passed, tsc clean, verify:drills 276 passed
- [x] Step 8 (§4, §5, §7.7) — service and attribution screen — new entity-scoped screen at /entity/:code/service (h1 'Where the delays come from', eyebrow 'Service & attribution'): (0) carry-over — EntityHome's last hardcoded JGL figure, the 'N systemic' span beside '38 resolvable today', now reads causeBacklog(m.causeElimination!).notStarted (§7.18: causes identified with no structural fix underway — JGL 3 / JBL 4 / JPS 2 / JCP 1 / JHS 2 / JRP 5), keeping the clearing-exceptions-vs-eliminating-causes pairing intact on all six homes; (1) §5 stacked bar of SLA breaches by origin from group-level slaBreachSplit() — client 101 / provider 25 / system 10 / third party 6 of 142 — with a render-only merge that folds any segment under 5% raw share into its predecessor, so the four stored counts (kept four: collapsing would break the group-total ↔ per-entity-row tie) render as three segments reading 71 / 18 / 11 with the last labelled 'system & third party'; segment colours accent → ageingBarAlt → borderAccent → textFaintest (no new tokens), a legend row labels every segment swatch + name + count · rounded %, and the card is tagged read-only · source: SLA breach log (§8.4); (2) §4 two-object panel — Health score (whole-card Link to the entity home, statusWord badge, CAPPED span where bound, HEALTH_SCORE_DEFINITION) beside Service scorecard (serviceScorecard() = 95.2%, SERVICE_SCORECARD_DEFINITION, 'Service credits attach here, and only here') — separate objects with their own definitions, never a merged number; (3) §7.7 SLA table via DataTable over the existing P2P/O2C service & control datasets in mock/misc.ts (extended, no third parallel dataset): ten rows — five day-one (target / achieved / breaches / attribution split rendered from data) and five needs-* (three needs-front-door, two needs-register) greyed with '—' value cells carrying title tooltips ('Not measurable yet — no clock start today; the request front door will supply it.' / '…requires manual register input.') — 15 unmeasured dashes, zero fabricated values, measurability stays data-driven so Step 13 can flip needs-front-door rows to live; (4) registration per §9.1: routes.tsx import + breadcrumb case (parts[2]==='service') + NavKey/activeNavKey + NAV_ITEMS entry carrying group:'SERVICE', CommandPalette SCREEN item with meta '142 SLA breaches' (query 'attribution' matches only it — vendor names contain 'services'), verify-drills.mjs item 7 asserts h1 / three .fct-sla-seg / every .fct-sla-unmeasured === '—' / breadcrumb / active nav and screenshots both themes; judgment calls: serviceScorecard() = mean of the five day-one achieved % (95.2) — §4 names the object but not its formula, and the mean is the only figure derivable from stored data without inventing one, h1 copy 'Where the delays come from', SERVICE group expressed as a data-model field on NavItem with Rail.tsx left flat (out of named scope; visible rail grouping awaits the full §9.1 reorg), legend row instead of in-segment text (a 7% segment cannot hold a label), SLA table and scorecard are reference data shown identically on all six entity pages (the JGL-reference-data precedent) while the bar is group-level — forced by the task's own 71/18/11 numbers; tests: api.test.ts gains a 'service scorecard (§4)' describe (95.2 pinned, five day-one rows, every needs-* row has achieved/breaches undefined), new tests/service-attribution.test.tsx with six tests — route + breadcrumb + active nav via the user path (group → JGL → rail item), bar merge/legend/read-only tag, two-object separation (health is a link to /entity/JGL, scorecard is not a link and carries its definition + credits line), SLA table ten rows + 15 dashes each titled 'Not measurable yet…' + 20 tooltip-bearing cells, unknown-entity fallback via pushState before render, palette registration navigating on Enter; gates: npm test 225 passed (13 files), verify:theme 41 passed, tsc clean, verify:drills 286 passed
- [x] Step 8b (§4, §7.22) — gross vs net, and per-entity service data — two corrections from the Step 8 flags: (1) serviceScorecard() now returns {gross, net} per entity — gross is the mean of the five day-one achieved % (the contractual figure), net applies the task's attribution formula per SLA, 100 − (100 − achieved) × providerShare, then averages; a row with zero breaches has nothing to net out and keeps its achieved value, and a row whose breaches are all non-provider nets to exactly 100 — mechanical consequence of the formula; JGL gross 95.2 / net 98.6 (the pinned 3.4-point gap), generated entities JCP 98.8/98.9, JHS 97.6/98.5, JPS 96.8/98.6, JBL 94.0/98.2, JRP 92.4/97.9; the scorecard card shows both side by side with unambiguous labels ('Gross achievement' / 'Net of client & third-party delay') plus the sentence 'We met X% against the contract; Y% of what we control.' — never net alone, that reads as excuse-making; (2) per-entity service data per §7.22 — mock/misc.ts gains serviceMetricsByEntity: JGL's five day-one rows stay the pinned §7.7 literals and are never regenerated, the other five entities generated deterministically so all four constraints hold exactly for every entity — (a) per-SLA breaches sum to the entity's slaBreachCounts total via largest-remainder allocation over JGL-shaped row weights [22/32, 4/32, 3/32, 2/32, 1/32] (ties → earlier row), (b) per-SLA attribution sums to the entity's stored counts via greedy margin-fill walking rows in order over client→provider→system→thirdParty (provably exact: both sides sum to the entity total), (c) gross mean = the entity's §7.22 service-dimension value by zero-sum achieved offsets [+0.6, −0.2, +0.5, −1.4, +0.5] over the five rows (sub-ledger close stays weakest, mirroring JGL), (d) needs-front-door / needs-register rows stay unmeasurable on every entity — no clock start anywhere; getServiceMetrics(entityCode) replaces the no-arg accessor; screen: the bar now shows the entity's own split (slaBreachSplit(entity.code)) with a comparison line beneath in legend order, 'JGL 72 / 19 / 6 / 3 · group 71 / 18 / 7 / 4' — which entities are worse than the group is the first question a controller asks; scope note reads '{code} · {total} breaches'; day-one rows with zero breaches render a dimmed '—' in the attribution cell (nothing to attribute); tests: api.test.ts 'service scorecard (§4, §7.22)' — JGL pinned {gross 95.2, net 98.6}, JGL's five day-one rows deep-equal the §7.7 literals, per-entity loop over all six asserting breach sums === slaBreachSplit(code).total, attribution column sums === stored counts, gross mean === §7.22 value, needs-* rows valueless; service-attribution.test.tsx — bar test rewritten for JGL's own split (titles 'client: 23 of 32' / 'provider: 6 of 32' / 'system & third party: 3 of 32', legend 72/19/9%, scope note, comparison line pinned to the literal string), scorecard test asserts gross + net values and both labels plus the sentence scoped to the card section, new JRP second-entity test (pushState '/entity/JRP/service') proving per-entity data — 'client: 33 of 48' / 'provider: 8 of 48' / 'system & third party: 7 of 48', comparison line 'JRP 69 / 17 / 10 / 4 · group 71 / 18 / 7 / 4', scorecard {gross 92.4, net 97.9}; verify-drills item 7 gains three checks per theme (comparison line present, gross+net labels and values in the scorecard section, attribution sentence); judgment calls: generator shape — JGL-shaped breach weights + fixed zero-sum achieved offsets (§7.22 authorizes generating per-entity SLA values against its constraints rather than pinning 30 values; offsets keep sub-ledger weakest like JGL's story), comparison line uses rounded percentages for both entity and group (both from slaBreachSplit .pct — 'JGL 72 / 19 / 6 / 3' is exactly JGL's pct since {23,6,2,1} of 32 rounds to those values), the net formula nets out ALL non-provider delay (client, system, third party) — broader than §4's 'net of client-caused and third-party delay' prose; followed the task's explicit formula because it reproduces JGL's pinned 98.6 / 3.4-point gap exactly, rows with provider=0 and breaches>0 net to exactly 100 (mechanical consequence — makes generated entities' gaps grow with weakness: JCP 0.1 → JRP 5.5); gates: npm test 227 passed (13 files), verify:theme 41 passed, tsc clean, verify:drills 292 passed
- [x] Step 9 (§7.8, §8.8, §1) — risk and control screen — two carry-overs + a new top-level group-scoped screen at /risk-control (h1 = the framing statement 'Everything the auditor will find, ninety days earlier.', eyebrow 'Risk & control'): (0a) service scorecard exclusion set — on-screen label now names it exactly, 'Net of client, system and third-party delay' (the formula was right; §4's prose was wrong), plus a contract-term note inside the scorecard section: 'The exclusion set is a contract term — whether an interface failure stops the clock depends on who operates the interface; for {code} the two readings differ by N points.' — altNet = per-SLA 100 − (100−achieved)×((provider+system)/breaches), mean, round1: JGL net 98.6 vs altNet 98.0 → Δ0.6 (the pinned figure), JBL 98.2/96.9 Δ1.3, JPS/JCP/JHS Δ0; SPEC.md §4 line 219 edited to 'net of delay the provider does not control' — flagged: revert that one line if you'd rather keep the spec as-is; (0b) light statusRed #D33C3C → #C22E2E (5.64:1 on white, 5.17:1 on bgRiskSoft — AA for small text where red-on-tint first appears in volume; tints and dark theme untouched), tokens.test.ts now asserts ratio(statusRed, bgRiskSoft) ≥ 4.5 in both themes with the sizing-caveat comment removed, frozen baselines updated (LIGHT_MODE_PLAN.md + PROJECT-MAP.md tables); data: src/api/mock/controls.ts — the eight §7.8 rows transcribed verbatim (the two system rows carry no valueAtRiskCr), detection dates fixed literals Jul/Aug 2026 (§7.8 pins none — flagged), apEffectiveness counts {duplicate 41 flagged / 2 overridden, threeWayMatch 67 failed / 5 overridden, valuePreventedYtdCr 23.4} (no SPEC-pinned values — deterministic choice, flagged); index.ts: getControlSignals(), serviceScorecard() now returns {gross, net, altNet}, apControlEffectiveness() computes override rates from the stored counts via Math.round(x*1000)/10 (rates computed, never stored), re-exports ControlCategory/ControlSignal/ApControlEffectiveness; screen: RESTRICTED banner ('RESTRICTED — Financial Controller and above', bgRiskSoft + statusRed) with a demo-only access toggle defaulting to visible — 'FC AND ABOVE' / 'PLANT MANAGER' bordered mono buttons mirroring the TopBar DEMO MODE pattern; when restricted, the sections are replaced by a notice (the banner text then appears twice); no-duplication note ('Where the source system already enforces a control… Control of the control, not a second control.') stays visible even when restricted — framing, not restricted content (§1 'What it is NOT'); five category sections in spec order (Payment / Authority / System integrity + Cut-off integrity + Undisclosed exposure), each signal row = title + severity tag via the existing controlColor() (High→statusRed / Medium→statusAmber) + detail + mono meta line 'value at risk {formatCr | —}' · entityCode · 'detected {date}'; effectiveness section last — 'Control effectiveness — AP automation', tagged read-only · source: AP tool control log, three metrics: duplicate override rate 4.9% ('2 of 41 flagged overridden'), three-way match 7.5% ('5 of 67 failed overridden'), value prevented ₹23.4 cr — monitoring the AP tool's controls only, no duplicate checking implemented (§7.8 item 6); registration per rule 16: routes.tsx ×4 (import, breadcrumb early return [Group → Risk & control], NavKey + activeNavKey early returns, NAV_ITEMS entry with group:'ASSURE' between rootCause and workingCapital — Rail.tsx unchanged, the flat rail renders in order), CommandPalette SCREEN item 'Risk & control' meta '{n} open signals', verify-drills.mjs new item 8 (framing h1 / banner / no-dup note / five categories in spec order / all eight titles / six values + two dashes / metrics / breadcrumb / active nav, then PLANT MANAGER hides → FC AND ABOVE restores, then §8.8 negative checks on both JGL cockpits — 'SoD conflict' and 'bank detail changed' must not appear) + item 7 gains exclusionNote/deltaLine checks; verify-theme.mjs ROUTES + EXPECTED_PRESENT ['statusRed','bgRiskSoft'] (route addition not named in the task's registration list — flagged); tests: new tests/risk-control.test.tsx with eight tests (route/breadcrumb/nav via the user path, banner+toggle+six sections in order, all eight signals with severity/value/entity/date, no-dup note, metrics, toggle hide/restore + notice, §8.8 non-leak on both cockpits via pushState render, palette registration navigating on Enter); api.test.ts — scorecard toEqual {gross 95.2, net 98.6, altNet 98} (JGL) / {92.4, 97.9, 95.8} (JRP), per-entity NET_MEANS/ALT_NETS pins for all six, new 'control signals (§7.8)' describe (exact eight-row transcription, category order + exactly the two system rows valueless, JRP bank-change veto cross-ref) and 'AP control effectiveness' describe (rates computed from counts); service-attribution.test.tsx — exact label + note strings for JGL ('differ by 0.6 points.') and JRP ('2.1 points.'); judgment calls: h1 = the framing statement (task item 4 says 'a framing statement at the top of the screen'), toggle defaults to visible / FC-and-above, valueless rows render as dashes, delta note is per-entity dynamic — JPS/JCP/JHS read 'differ by 0 points', effectiveness metric values chosen deterministically; gates: npm test 239 passed (14 files), verify:theme 43 passed, tsc clean, verify:drills 320 passed
- [x] Step 10 (§7.3, §8.6, §7.21, §7.8.1) — predictive screen — three carry-overs from step 9 + a new top-level JGL-scoped screen at /predictive (h1 = live headline 'DSO 62 today → 72 projected at month-end', eyebrow 'Predictive'): (0a) the eight control-signal detection dates in mock/controls.ts now derive as fmtDate(addDays(ANCHOR, −N)) per §7.21 — N = bank-change 12 / first-payee 21 / po-split 34 / retro-po 46 / SoD conflict 28 / terms change 40 / cutoff GRs 30 / GRNI ageing 25 (spec pins no dates — flagged); audit found these eight were the only remaining date literals in the product; (0b) §7.8.1 effectiveness figures pinned with a comment — duplicate check 41/2 → 4.9%, three-way match 67/5 → 7.5%, value prevented ₹23.4 cr YTD (values already matched, so only comment + describe wording changed); (0c) ServiceAttribution's exclusion-set delta note suppressed when Δ = 0 — JPS/JCP/JHS have no system-attributed delay and a zero difference is not a caveat; data: types.ts ForecastDriver gains baseSettleOn/baseSettleIso ('YYYY-MM-DD', lexicographic = chronological), DriverAssumption {resolved?, settleIso?} (empty string = cleared → falls back to base); mock/misc.ts's four §7.3 drivers gain assumed settlement dates via a baseSettle(n) helper — endOfMonth(ANCHOR) + n days, n = 9/5/3/2 for Customer A/B/C/cash (spec pins no dates — flagged: any n > 0 keeps the pinned 62 → 72 headline holding whenever opened); index.ts gains monthEndIso(), driverOpenAtMonthEnd(d, a) — resolved drivers closed, settlements on or before month-end closed — and projectDsoDays(forecast, assumptions) = current + Σ impact of open drivers (1dp): the single decision point used by both the projection and the page's state tags; screen: live h1 recalculating as assumptions change; JGL scope tag + RESET TO BASE CASE button in the header; DPO section — Metric with trend from entity metrics, inverse null (§8.5.1 direction-neutral) + §8.6 footnote 'Includes ₹18.6 cr of blocked invoices; adjusted DPO 41 days.' composed from the same entity.metrics as Working capital (figures cannot drift), tagged read-only · source: entity metrics; drivers DataTable — driver / value formatCr / day impact '+N.N' / assumed settlement date input (disabled when resolved) / state tag + MARK RESOLVED ↔ UNDO control per row (§7.3 'a forecast a controller can contest'); ranked actions DataTable in spec order with the note 'ranked by days recovered per unit of effort, not by size of improvement'; registration per rule 16: routes.tsx ×4 (import, breadcrumb [Group → Predictive], NavKey + activeNavKey, NAV_ITEMS entry group:'FORWARD' after workingCapital — sole member; Rail.tsx unchanged), CommandPalette SCREEN item 'Predictive' meta 'JGL · DSO 62 → 72 at month-end', verify-drills.mjs new item 9 (base headline / §8.6 footnote / four drivers + values + impacts / actions in spec order + effort note / four OPEN AT MONTH-END tags, then clickByText('MARK RESOLVED') — first DOM match is Customer A's row by data order → '→ 67.9', then a native-setter date probe on the third input (Customer C) to first-of-month → '→ 65.8', then RESET TO BASE CASE restores headline + all four settlement dates past month-end, breadcrumb + active nav, screenshots both themes), verify-theme.mjs ROUTES + EXPECTED_PRESENT ['statusAmber'] (route addition not in the task's registration list — flagged); tests: new tests/predictive.test.tsx with seven tests (route/breadcrumb/active nav + base headline; DPO section '48 days' + exact footnote; four drivers with values/impacts/base ISOs + four OPEN tags; actions in spec order + note + owners; live recalculation — resolve A → 67.9, UNDO → 72, re-resolve + pull C to first-of-month → 65.8; reset restores headline + base dates + four OPEN tags; palette 'predictive' → one row → Enter navigates); api.test.ts gains a 'DSO forecast (§7.3)' describe — verbatim transcription (headline, four drivers with labels/values/impacts, assumptionEditable true, four actions in spec order), the invariant that every base settlement sits after monthEndIso() so the pinned headline holds whenever opened (§7.21), projection cases {} → 72 / resolve A → 67.9 / settle C early only → 69.9 / all resolved → 62, and driverOpenAtMonthEnd boundary cases (on-month-end = settled; cleared override falls back to base); judgment calls: settlement N values 9/5/3/2 days after month-end (spec pins none — they need only sit past month-end), top-level JGL-scoped route rather than per-entity routes (§7.3 pins the forecast to JGL only; generating four more entities would invent numbers at scale, §7.22 lesson), binary open-at-month-end model (a driver either counts or it doesn't — no partial credit for mid-month settlements), h1 = live headline (task item 1 is the screen's headline and item 4 makes it move), 'FORWARD' group metadata only on predictive while workingCapital stays ungrouped (Rail renders flat; full §9.1 grouping awaits step 16), reuse of the existing Forecast/ForecastDriver/RankedAction types seeded in an earlier step rather than new names, verify-drills' MARK RESOLVED first-match relies on DataTable rendering rows in data order with Customer A first; gates: npm test 251 passed (15 files), verify:theme 45 passed, tsc clean, verify:drills 344 passed
- [x] Step 10b (§7.23) — forecasts for the other five entities — the Predictive screen now works for every entity: data — mock/misc.ts's `forecasts` grows from one entry to six in severity order JRP/JBL/JGL/JPS/JHS/JCP, transcribing §7.23's table (JRP 74→86 +12 ≈₹22.2 cr · JBL 68→79 +11 ≈17.2 · JGL 62→72 +10 ≈23.5 pinned · JPS 54→59 +5 ≈9.6 · JHS 51→55 +4 ≈6.7 · JCP 47→50 +3 ≈3.3) with the generation constraints holding exactly — day impacts sum to the deterioration per entity, driver values sit around 1.5× (AR over 90 + cash unapplied) for every entity (actual ratios all in [1.5, 1.53]; test band [1.45, 1.6]), JGL's four drivers/actions keep their pinned §7.3 numbers unchanged and only gain real customer names from the India pool, actions are ranked by days recovered per unit of effort (Low=1 / Medium=2 / High=4) strictly descending for all six entities, and every base settlement sits after month-end via the existing baseSettle(n) helper so each pinned headline holds whenever opened; types.ts — ForecastDriver gains `id: string` (stable identity; rows and controls are selected by id, never by position); index.ts — projectDsoDays now keys assumptions by d.id instead of label (label-keying leaked resolved-state across entities because the route component is reused when only :code changes and 'Cash awaiting application' exists in all six forecasts); screen — Predictive.tsx reads useParams(), keeps its single useState above the unknown-entity guard, keys all assumption lookups by id, renders data-driver-id={d.id} on each MARK RESOLVED/UNDO button (DataTable exposes no row-level attributes and was not modified), drivers table rowKey r.id, h1 + scope tag follow the entity; registration per rule 16 — routes.tsx ×4 (/entity/:code/predictive replaces top-level /predictive with no redirect; breadcrumb [Group → code → Predictive]; activeNavKey case 'predictive'; NAV_ITEMS to(c) => /entity/${c ?? DEFAULT_ENTITY}/predictive), CommandPalette row follows the entity in context (meta `DSO {current} → {projected} at month-end`) and is omitted for unknown codes — getForecast(unknown) is undefined and the fallback page has no forecast, which also fixes a crash the gate run surfaced on /entity/ZZZ pages; verify-drills item 9 rewritten off 'first matching row' onto the stable [data-driver-id] selector (date input located via .closest('.fct-table-row')) plus new JRP-scope checks — navigating to /entity/JRP/predictive shows 'DSO 74 today → 86 projected at month-end' with Mont-Royal Imaging and no Amrit Distributors; verify-theme.mjs route entry now /entity/JGL/predictive; tests — predictive.test.tsx rewritten (new per-entity test: JRP renders its own h1/drivers/breadcrumb and the rail's Predictive link targets /entity/JRP/predictive; interactions select controls by data-driver-id; palette meta follows context) and api.test.ts's describe becomes 'DSO forecast (§7.3/§7.23)' with nine tests — JGL verbatim transcription (now including ids + real names), §7.23 table loop (current DSO ties to entity metrics, impacts sum exactly to the deterioration), deterioration strictly inverse to the working-capital dimension (JRP +12 worst at WC 48 → JCP +3 best at WC 86), 1.5× band for all six entities, effort ranking strictly descending for all six entities with sequential ranks, base settlements after month-end for all six entities, no 'Customer X' placeholders + unique entity-prefixed ids, projection keyed by id ({} → 72 / jgl-amrit resolved → 67.9 / jgl-deccan settled early → 69.9 / all resolved → 62), driverOpenAtMonthEnd boundary cases; judgment calls: settlement N values after month-end are a deterministic choice the spec pins nowhere (JRP 10/6/4/2 · JBL 8/5/2 · JGL 9/5/3/2 unchanged from Step 10 · JPS 7/4/2 · JHS 6/3/2 · JCP 5/3/1), top-level /predictive dropped without a redirect (no in-app link survives — palette and rail both emit per-entity URLs), the `id` field added to ForecastDriver as the stable-selection mechanism item 5 requires, L=1/M=2/H=4 effort weighting is the checkable reading of 'per unit of effort' (validated against JGL's pinned spec order before being applied to the generated entities), assumption keying label→id in index.ts + Predictive extends scope beyond the named files but is forced by item 4 (the leak it fixes is a correctness bug, not a refactor), JGL action labels updated where they referenced renamed customers ('Release credit block on Deccan Pharma Retail' / 'Escalate Amrit Distributors to commercial') — §7.23 authorizes real names over the §7.3 placeholders while the numbers stay pinned; gates: npm test 257 passed (15 files), verify:theme 45 passed, tsc clean, verify:drills 350 passed
- [x] Step 11 (§6, §7.24, §8.8) — counterparty pages as first-class objects — process views answer 'how is P2P performing'; these four pages answer 'what is happening with this vendor', so the phone call no longer deflects to the service desk: data — new src/api/mock/counterparties.ts deriving everything from existing datasets (no parallel module): vendors from the §7.17 exception rows in first-appearance order, blockedCr = integer-paise group sum of that vendor's row amounts; openCommitmentsCr allocated by largest-remainder over the PO stage inFlightValue (₹58.4 cr) with weights blockedPaise+100 so Σ ties exactly per entity; ytdSpendCr / lastPaymentDate seeded mulberry32(fnv1a(`${code}:vendor:${name}`)) under §7.22/§7.23 authorization; customers = the §7.23 forecast drivers already named (id = driver id, so a Predictive driver row drills to a page that exists), exposureCr from the driver value, creditBlocked when issue === 'credit block', releasePath composed `${release.action} — ${release.owner}`; plants from the §7.17 site list with JGL special-cased — its twelve pinned rows cannot satisfy §7.5's 43/29/18/10, so blockedCr = largest-remainder allocation of the worklist shown total (1277 paise) by the missing-gr cause plant pcts → [549, 370, 230, 128], other entities group-sum their rows; grCompliancePct = round(98 − 0.12×sharePct, 1dp); cost centres are the only genuinely new dataset — the §7.24 list (JGL: Roorkee Operations 62/41.2/19.6 · Nanjangud Operations 71/47.5/23.8 over budget −0.3 · Quality & Regulatory 24/15.8/7.2 · Corporate 26/12.4/7.8, plus JBL/JPS/JCP/JHS/JRP centres), each centre's openPos summing to its committedSpendCr in paise and Σ committed = PO stage in-flight per entity; types.ts gains Counterparty / PlantCounterparty / CostCentre, index.ts exposes listCounterparties(entityCode?, type?) / getCounterparty(id) / listPlants / getPlantDetail (worklist rows at the plant + blocked value by cause, paise sums sorted desc) / listCostCentres plus vendorRoute/plantRoute slug helpers returning undefined when no counterparty exists so cells fall back to plain text; pages — VendorPage (every open item carries its owner and reason, blocked value by age bucket, last payment, YTD spend, open commitments), CustomerPage (exposure, disputes & deductions, payment behaviour, CREDIT BLOCKED status + the release path), CostCentrePage (booked AND committed against budget — remaining after commitments = budget − booked − committed, over-budget footnote in statusRed, the open PO table driving the gap: 'the part most tools miss'), PlantPage (blocked value, GR compliance, open items, vendor spread, ageing by cause; item rows drill to invoice + vendor; §7.20 partial-sample footnote where shown rows do not cover the plant figure); registration per rule 16 — routes.tsx ×4: four route entries before the catch-all + breadcrumb cases (vendor → Group→entity→P2P→name, customer → O2C variant, cost-centre/plant → P2P) and activeNavKey vendor/plant/cost-centre → 'worklist', customer → 'o2cCockpit'; NO NAV_ITEMS entries (§9.1 — drill-only); CommandPalette gains VENDOR / CUSTOMER rows via listCounterparties, PLANT via listPlants, COST CENTRE via listCostCentres, scoped to the context entity (entityCodeFromPath ?? JGL) with value metas; verify-drills.mjs new item 10 (four h1 loads per theme / worklist vendor + plant cell hrefs then an in-page synthetic .click() navigation — a coordinate click after a fresh full load landed on stale layout and never navigated, so the drill uses the same deterministic pattern as the palette rows / root-cause by-plant row href / P2P-cockpit cost-centre row href (textContent includes its value span → .includes match) / ⌘K 'suraksha' → exactly two rows, EXCEPTION first and VENDOR second, in-page click of the VENDOR row navigates — ENTER would pick the exception) + the §8.8 negative loop extended from two routes to six (all four counterparty routes must not contain 'SoD conflict' or 'bank detail changed'); drill entry points — Worklist vendor + plant cells become Links via the route helpers, RootCause by-plant DriverRow links for p2p causes, and a new P2PCockpit section 'Committed spend by cost centre' (each centre row is a Link to its page; footer 'Open POs not yet invoiced — the part most tools miss' + committed total) — without it the cost-centre pages would be unreachable except via ⌘K; tests — new tests/counterparty.test.tsx with eleven tests: five §7.24 tie tests in integer paise (Σ vendor blocked = worklist shown per entity, JGL pinned 1277; Σ plant blocked same + JGL [549,370,230,128]; Σ cost-centre committed = PO stage inFlightValue per entity AND each centre's openPos sum to its own committedSpendCr; Σ customer exposure ≥ arOver90 for all six entities — JGL 20.4≥12.4 … JRP 20.9≥10.6; added fifth tie Σ vendor openCommitments = 58.4 per entity) + six page tests (vendor owner/reason/age, customer credit block + release path, cost-centre −₹0.3 cr over budget + full PO list, plant GR compliance 92.8% + partial-sample footnote 'Shown rows total ₹4.86 cr…' + vendor drill link, breadcrumb back to P2P, palette 'nanjangud' → PLANT + COST CENTRE rows → click navigates); rootcause-workingcapital.test.tsx updated (taxonomy links filtered by href prefix — the plant drill links raised 6→10; Nanjangud row now asserts its /entity/JGL/plant/jgl-nanjangud href) and palette-assistant.test.tsx 'suraksha' expects two rows; judgment calls: (i) JGL plant split vs pinned-row distribution tension resolved by allocating the shown total per §7.5 with a partial-sample footnote on the plant page (§7.20), (ii) every entity's cost centres sum committedSpendCr to the same 58.4 because stages.ts is one dataset rendered on all six P2P cockpits, (iii) the fifth tie Σ vendor openCommitments = PO in-flight added via largest-remainder allocation (weights blockedPaise+100) so a controller can check commitments against the stage too, (iv) seeded generation of ytdSpend / lastPaymentDate / customer ageing buckets under §7.22/§7.23 authorization, (v) palette counterparty rows scoped to the context entity rather than group-wide, (vi) the P2PCockpit cost-centre section added as the process-screen drill entry point; gates: npm test 268 passed (16 files), verify:theme 45 passed, tsc clean, verify:drills 380 passed
- [x] Step 11b (§7.25, §7.24) — per-entity stages and the plant tie — two corrections: (1) stages.ts held one dataset, so every cockpit rendered JGL's figures on all entities since Step 5 — now `stagesFor(entityCode)` scales the §7.4 base table per entity (P2P by apBlocked.current/18.6, O2C by arOver90.current/12.4; JGL's ratio is exactly 1 so its table stays byte-identical and every existing pin holds), then overrides the six pinned tie points: INV inException = apBlockedCount · INV exceptionValue = apBlocked.current · PO inFlightValue = §7.25 PO table (JGL 58.4 … JRP 46.2) · COL inException = o2cExceptionCount · COL inFlightValue = collection table (56.3 … 48.1) · CSH exceptionValue = cashUnapplied.current; listStages(processKey, entityCode?) defaults to JGL for existing callers; both cockpits pass their own code and the hardcoded button copy ('Open 327 blocked invoices →' / 'Open 41 overdue customers →') is now derived from entity metrics (JGL renders byte-identical); exception percentages stay derived via stageExceptionPct, never stored. (2) Plant tie corrected — plants are a fixed complete set, so they tie to the POOL: Σ plant blocked = entity blocked AP via integer-paise largest-remainder allocation by the §7.24 overall shares (JGL 43/29/18/10 · JBL 62/38 · JPS 100 · JCP 100 · JHS 58/42 · JRP 55/45), not the sample; vendors stay sample-derived and tie to the worklist shown value; §7.5's 43/29/18/10 is the within-missing-GR split and no longer applied to the whole pool; plant names reuse exceptions.ts site vocabulary so openItems matching stays exact; JGL Nanjangud page now reads '₹8.0 cr blocked' (GR compliance still 92.8%) with the partial-sample footnote unchanged ('Shown rows total ₹4.86 cr…'); vendor commitment allocation and cost-centre committed spend now source from the per-entity PO in-flight — non-JGL centres hand-split to sum exactly (JBL 35.5 · JPS 21.4 · JCP 6.6 · JHS 13.2 · JRP 46.2, each centre's PO list sums to its committed figure; JRP Regulatory kept at 10.6 so only JGL Nanjangud runs over budget); tests — new api.test.ts describe asserts all six tie points × six entities plus a regression guard that non-JGL tables differ from the JGL default; counterparty.test.tsx plant-tie test loops Σ plants = apBlocked pool per entity with JGL pinned at [800, 539, 335, 186] paise, cost-centre and vendor-commitment ties use per-entity PO stage lookups, plant header pin → '₹8.0 cr blocked'; judgment calls: (i) scaling ratios follow §7.25's stated guidance (PO with blocked AP, collection with AR over 90), (ii) plant names reuse the exceptions.ts vocabulary rather than the spec table's short forms, (iii) non-JGL cost-centre committed splits and openPos hand-picked to sum exactly since the spec pins only entity totals; gates: npm test 270 passed (16 files), verify:theme 45 passed, tsc clean, verify:drills 380 passed
- [x] Step 12 (§7.26, §7.27, §7.28) — compliance and data quality screens — two new group-scoped ASSURE screens + the jurisdiction fix they exposed: (0) JRP's overdue return was specified as GSTR-3B — an Indian GST filing on a US/Canada radiopharmacy entity; it is now a GST/HST return filed with the Canada Revenue Agency, and every place that named it follows: entities.ts veto reason 'GST/HST return overdue' (id stays gstOverdue), sensitivity action 'File the overdue GST/HST return', cascade text, and the tests that assert it — obligations match the entity's jurisdiction throughout per §7.26; (0b) §7.28 overruns — JBL Lab Operations (budget 60.0 / booked 44.2 / committed 16.8 → −₹1.0 cr) and JRP Regulatory (23.5 / 13.2 / 10.6 → −₹0.3 cr) now overrun, driven by committed spend rather than booked: the budget looks fine until you count the open purchase orders, which is the argument the cost-centre page exists to make; data — types.ts gains ComplianceItem (obligation/entityCode/dueDate/status filed|due|overdue/valueAtRiskCr?/evidenceRef? + optional failCount for count-based exposures), DataQualityItem (check/domain vendor|customer|gl|interface/entityCode/failCount/totalCount/impact) and InterfaceHealth (entityCode/status on schedule|delayed|stale/lastSuccessfulRun); mock/compliance.ts transcribes §7.26's 27 rows jurisdiction-matched per entity (JGL India 7 · JBL India 7 · JPS Singapore 3 · JCP US 3 · JHS US 3 · JRP US/Canada 4) with today = 2026-08-31 — FILED 19 / DUE 7 / OVERDUE exactly one (JRP's GST/HST return, due 2026-07-31, ₹1.2 cr at risk; every other entity's compliance dimension is ≥88 and an overdue filing would cap it at 60 under §3.5); value-at-risk figures pinned per the §7.26 table (JGL recon 1.8 / MSMED 2.4 · JBL 0.9 / 1.1 · JPS F5 0.4 · JCP sales & use 0.2 · JHS GST/HST 0.3); mock/dataQuality.ts — six checks with pinned JGL anchors (vendor PAN missing 14/812 'Blocks e-invoice validation' · duplicate vendors 9/812 · dormant 48/812 · customer tax reg 6/430 · GL cost-centre default 3/210 · failed IDocs 12) scaled per entity by (100 − dq)/10 anchored at JGL's 90 → JCP ×0.6 … JRP ×2.6, so Σ fails orders strictly inversely with the data-quality dimension (55 < 73 < 92 < 111 < 165 < 239; grand total 735) and interface health rows pin last successful runs from '31 Aug 2026, 04:00' (JCP on schedule) to '27 Aug 2026, 16:35' (JRP stale); index.ts exposes listCompliance(entityCode?) / listDataQuality(entityCode?, domain?) / getInterfaceHealth(entityCode); derive.ts gains complianceColor(status) and interfaceColor(status) — colour derivation stays in one place; screens — CompliancePage (/compliance, h1 'Every statutory obligation, in the jurisdiction that owns it.') renders per-entity sections with a JURISDICTION column (multi-jurisdiction coverage is the selling point), status tags via fct-status-tag class so counts are exact (FILED 19 / DUE 7 / OVERDUE 1) and value-at-risk or failure counts; for JRP an OverdueChain strip makes the link explicit — overdue filing → compliance dimension → veto cap on the entity score, three segments each a Link to /entity/JRP, with capText computed from the active vetoes: for JRP the bank-change veto (cap 55) binds tighter than the GST/HST cap of 60 and the text says so rather than claiming 60; DataQualityPage (/data-quality, h1 'Where master data fails — and the working capital it becomes.') groups checks by domain in spec order (Vendor / Customer / General ledger / Interfaces), each check row shows fail/total per entity with its downstream impact, the interface section adds per-entity health rows (status tag · last successful run · failed IDocs 7d), and a final 'Master data → working capital' panel links the vendor-master failure to the root-cause view — figures composed at render time from getCause('vendor-master') (11% of JGL's blocked AP, ₹2.0 cr) so master data quality shows up as working capital; registration per rule 16 — routes.tsx ×6 (imports, breadcrumb cases [Group → Compliance] / [Group → Data quality], NavKey + activeNavKey early returns, NAV_ITEMS entries group:'ASSURE' after riskControl, Route entries before the catch-all), CommandPalette SCREEN items with dataset-derived metas ('8 open obligations' = non-filed rows · '735 failing records' = Σ failCount), verify-drills.mjs new items 11–12 (compliance: h1 / all four jurisdictions visible / JRP-only overdue via tag counts / filed+due 26 of 27 / eight value-at-risk figures / e-invoice failure counts / three chain links to /entity/JRP / breadcrumb + active nav; data quality: h1 / four domain labels / '14 / 812' anchor / six impact strings / all three health statuses / both extreme last runs / cause-link href + §7.5 figures / breadcrumb + active nav), verify-theme.mjs ROUTES + EXPECTED_PRESENT ['statusGreen','statusAmber','statusRed'] for both routes (route additions not named in the task's registration list — flagged per Steps 9/10 precedent); tests — new tests/assure-compliance-dq.test.tsx with thirteen tests: JRP-only overdue + every other compliance dimension ≥88, jurisdiction match (no GSTR* obligations outside India; India carries GSTR-1 + TDS deposit), value-at-risk transcription of the §7.26 table, e-invoice counts 14/8/21, strict inverse ordering of fail totals vs dq score (§7.27 item 5 asserted), JGL anchor pins (six rows / four domains / PAN 14 of 812 / IDocs 12), vendor-master cause tie (sharePct 11 · valueAtRisk 2.0), both screens' render + breadcrumb + active-nav tests, and palette registration navigating on Enter for each; judgment calls: (a) optional failCount added to ComplianceItem — §6's type has no count field and valueAtRiskCr is ₹cr, so the e-invoice IRN failure counts needed a home, (b) JRP's 21 e-invoice failures ride on its Sales & use tax row because that is where §7.26 lists them, (c) IDoc total = 580 — the spec pins only the fail count, not the denominator, (d) JHS geography stays 'United States' per entities.ts and the spec entity table while §7.26's label reads "US / Canada" — discrepancy flagged, dataset untouched, jurisdiction displayed from Entity.geography, (e) the chain capText states honestly that for JRP the bank-change veto binds tighter than the GST/HST cap of 60; gates: npm test 285 passed (17 files), verify:theme 49 passed, tsc clean, verify:drills 418 passed
- [x] Step 13 (§6, §7.29, §7.26, §7.7) — Finance Service Desk — the request intake that gives roughly eight committed SLAs a clock start; two jurisdiction corrections first: (0a) JHS geography in entities.ts is now 'US / Canada', matching its Spokane and Montreal sites (§2 was the wrong one — Step 12's flag 4 confirmed), (0b) e-invoice IRN failures are India-only — JGL 14 · JBL 8 keep the 'IRN failures' label, while JRP's 21 become Form 1099 TIN mismatches with JCP 9 and JHS 6 on their filed 'Form 1099 filings' rows (same error class as GSTR-3B, one table over); ComplianceItem gains failureLabel? so the screen renders `${failCount} ${failureLabel}` — 'IRN failures' vs 'TIN mismatches'; data — types.ts Request per §6 (id / type six values / entityCode / raisedBy / raisedOn — the SLA clock start / category / owner / status four values / clockStoppedHours / resolvedOn?), new mock/requests.ts with deterministic seeded generation (fnv1a+mulberry32; deflection on its own `deflection:${code}` stream) and VOLUME {JRP 12 · JBL 11 · JGL 10 · JPS 9 · JHS 8 · JCP 8} = 58 tracking exception volume (JRP/JBL busiest, JCP lightest); a seeded Fisher-Yates shuffle guarantees every entity's first six rows cover all six types and its first four are a permutation of the four statuses; raisedOn = ANCHOR − k per §7.21 (closed k ∈ [8,37], open k ∈ [1,25] — ages date-independent within the seed); clockStoppedHours 4–63 h accruing only while awaiting-client (§5's attribution logic applied to requests; closed rows always 0); TYPE_OWNER maps each type to an owning team (query 'Service desk' · dispute 'Collections' · masterData/fixedAsset 'R2R tower' · priceChange 'P2P tower' · urgentPayment 'Cash application'), plus a RAISED_BY client-role pool and four CATEGORIES per type; index.ts — listRequests(entityCode?, type?), requestTypeForSla (the three desk SLAs → masterData/query/dispute, undefined for the rest), requestAgeDays (closed rows age to resolvedOn, open to ANCHOR) + effectiveAgeDays = max(0, age − stoppedHours/24), serviceDeskStats(entityCode?, type?) {open, oldestEffectiveDays 1dp, avgResolutionDays, stoppedHours}, deflection(entityCode?) — routed = request count, selfServed seeded per entity under §7.29's counter authorization (group 34 of 92 → ratePct 37, computed never stored), serviceDeskWindow() {measuringSince: fmtDate(ANCHOR − 5) '26 Aug 2026', nextPeriod 'SEP-2026'} with PERIOD_MONTHS the single derivation site, requestSlaStatus(r) — undefined for no-TAT types or a missing/NaN target, else parseInt(target) days into derive.ts's new requestSlaStatusWord(effectiveAgeDays, targetDays, closed) (closed ≤target 'met' / >target 'breached'; open >target 'breached' / else 'on track') + requestSlaColor (breached statusRed · met statusGreen · on track textSecondary); §7.29 transition — the three needs-service-desk SLAs ('Vendor master creation', 'Query resolution', 'Dispute resolution') flip to measurability 'measuring' with achieved/breaches left undefined: they leave the greyed state and show live current-period-to-date figures but report no achievement % and do not enter breach totals or the attribution split — §7.11's pinned per-entity totals {JGL 32 · JBL 41 · JPS 12 · JCP 3 · JHS 6 · JRP 48} (group 142, 71/18/7/4) asserted as a regression guard in the new test file; the two needs-register SLAs ('Invoice processing accuracy', 'Audit findings') stay greyed — a service desk does not give you audit findings or a QC sample; screen — src/pages/ServiceDesk.tsx at /service-desk (group-scoped): h1 'The clock starts here.' under eyebrow 'Service desk' (a plain div header, not <header>), the pitch line 'a single structured intake for every request into the service — query · dispute · master data · fixed asset · price change · urgent payment', and the honesty line in mono textFaint `measuring since 26 Aug 2026 · first full-period report from SEP-2026` (you cannot report full-period performance on something begun mid-period); stat strip OPEN REQUESTS 45 / OLDEST EFFECTIVE AGE '25 d' / STOP-CLOCK HOURS 416 / SELF-SERVE SHARE 37% — all computed, never stored; 'New request' form tagged 'demo · writes to local state only' (ENTITY/TYPE selects + RAISED BY placeholder 'Client role, e.g. Plant stores' + CATEGORY) whose submit builds a row with id REQ-{code}-S{seq}, raisedOn from local date parts (not toISOString — UTC would shift the day), owner 'Unassigned', status open, clockStoppedHours 0 and appends it to session state only; queue DataTable of all 58 rows sorted effectiveAgeDays desc with columns Entity/Type/Category/Raised by/Raised on/Age d/Stopped h/Owner/Status (uppercased)/SLA status — the three TAT types render their word coloured via requestSlaColor, the other three carry a dimmed dash titled 'No committed TAT for this type'; deflection table of six entity rows + GROUP with columns Entity/Self-served/Routed to the service team/Self-serve share; registration per rule 16 — routes.tsx ×4 (import, breadcrumb [Group → Service desk], NavKey + activeNavKey early returns, NAV_ITEMS entry group:'SERVICE' after serviceAttribution), CommandPalette SCREEN item 'Service desk' meta '{n} open requests' from the dataset (seeded-only, unaffected by form submits), verify-drills.mjs new item 13 — 23 checks per theme: h1 / pitch phrase / exactly one measuring row / exact window line / queue 58 rows + all six type labels + all four statuses / stat strip values / deflection 7 rows with GROUP at 37% / breadcrumb + active nav, then the form leg (native-setter selects JRP+dispute, raisedBy 'Drill probe', submit → 59 rows and the new row carries JRP/Dispute/Drill probe/Unassigned/OPEN), then palette 'service desk' → one row matching /\d+ open requests/ → click navigates; verify-theme.mjs ROUTES + EXPECTED_PRESENT ['statusRed','statusGreen','textFaint','borderDefault'] (route addition not named in the task's registration list — flagged per Steps 9/10 precedent); tests — new tests/service-desk.test.tsx with fourteen tests: dataset describe (per-entity counts {JGL 10 · JBL 11 · JPS 9 · JCP 8 · JHS 8 · JRP 12} within 8–12 summing to 58, all six types + four statuses per entity, stop-clock equivalence >0 ⟺ awaiting-client over every row, raisedOn ≤ today lexicographically with ages in [1,37], deflection routed = count with group {selfServed 34 · routed 58 · ratePct 37}, window mirrored node-side, SLA→type mapping + seeded rows for each desk type per entity, requestSlaStatus closed/open rule over every row, §7.11 flip guard — measuring set exactly the three desk SLAs with achieved/breaches undefined, needs-register pair untouched, per-entity totals pinned) and screen describe (route + breadcrumb + active nav + honesty line via h1.previousElementSibling because queue owner cells also read 'Service desk', queue 58 rows + '58 requests · 45 open' + six types + four statuses + stat strip, form submit via fireEvent.submit(form) → 59 rows / '59 requests · 46 open' with the new row carrying JRP/Dispute/Unassigned/OPEN, deflection 7 rows + GROUP at 37%, palette registration navigating on Enter); judgment calls: (a) clockStoppedHours asserted as a current-state equivalence (>0 ⟺ awaiting-client; closed always 0) rather than per-row accrual history — the seed has no event log to replay, (b) three request types carry no TAT SLA (fixedAsset/priceChange/urgentPayment have no §7.29 target) so their queue rows show a dimmed dash and only masterData/query/dispute map to desk SLAs, (c) TYPE_OWNER / RAISED_BY / CATEGORIES pools are invented reference strings — the spec names none ('Service desk' as an owner is new vocabulary), (d) deflection selfServed values seeded per entity on a separate mulberry32 stream under §7.29's counter authorization; the spec pins no numbers and the group lands at 37%, (e) form-submitted rows get owner 'Unassigned', raisedOn from local date parts, category falling back to TYPE_LABEL[type] and raisedBy fallback 'Unspecified', (f) nav label is 'Service desk' while the screen's spec name is Finance Service Desk (§7.29 allows the nav label to differ), (g) JHS/JCP tie at 8 requests — 'JCP lightest' reads as the bottom of the volume ordering, (h) status column renders uncoloured text and stopped hours in textSecondary — no new band derivation added to derive.ts, (i) verify-drills item 13's window line is built node-side and injected via JSON.stringify because a nested template literal inside evaluate() closes the outer template early; gates: npm test 299 passed (18 files), verify:theme 51 passed, tsc clean, verify:drills 466 passed
- [x] Step 14 (§7.5, §7.30, §7.29) — cause elimination backlog — two carry-overs first: (0a) request owners become named people from the owning entity's §7.17 pool rather than departments — mock/requests.ts gains OWNER_POOL (JGL P. Nair · A. Sethi · R. Iyer / JBL M. Kulkarni · S. Rao / JPS W. Tan · L. Cheong / JCP D. Whitfield · K. Moreau / JHS J. Halloran · T. Bergstrom / JRP C. Tremblay · N. Okafor) + exported requestOwnerPool(), and the owner is drawn on a separate seeded stream mulberry32(fnv1a('requests-owner:${code}')) so the pinned request-row stats stay untouched; the service-desk form test now asserts 'C. Tremblay' for a fresh JRP intake, (0b) deflection pinned at 34 self-served of 92 contacts = 37% — it carries into Step 16's operating-model argument alongside ₹23.4 cr prevented; data — types.ts CauseBacklogRow/CauseBacklogEntry, new mock/causeBacklog.ts with the 34 rows ordered JGL→JRP transcribing §7.18's per-entity tallies exactly (group 34 identified / 11 eliminated / 6 in progress / 17 not started), owners from the owning entity's pool, target dates only on in-progress rows via TARGET(days) = isoDate(addDays(ANCHOR, days)) — JGL 70/125 · JBL 45 · JPS 95 · JHS 150 · JRP 180 (inside the next two quarters per §7.21; a blank is more honest than an invented commitment), eliminatedInPeriod cumulative [3,5,6,8,9,11], and the period-6 pair CB-JCP-03 duplicate-suspicion/p2p generatedLastPeriod 18 + CB-JRP-07 tax-mismatch/p2p 29 = 47; index.ts — listCauseBacklog() joins getCause(row.causeKey) so name/valueAtRisk/recurrence come from the group taxonomy nodes, causeBacklogCounts() derives all four counts, causeEliminationTrend() {eliminatedSeries cumulative over periods 1–6, openExceptionsSeries [...earlyOpenExceptions [2158,2117,2069,2034], openExceptionsPrevious(), openExceptions()] — the last two points are live group values, not a second stored series}, currentPeriodEliminations() → {count 2, generatedLastPeriod 47}; derive.ts causeEliminationColor (identified textMuted · in-progress statusAmber · eliminated statusGreen) + CauseEliminationWord; screen — src/pages/CauseBacklog.tsx at /cause-backlog: h1 'Continuous improvement, evidenced.' under eyebrow 'Cause elimination', pitch line 'every identified root cause with a named owner and an elimination status', stat strip IDENTIFIED 34 / ELIMINATED 11 / IN PROGRESS 6 / NOT STARTED 17 (derived), register DataTable of all 34 rows — back-link to /entity/{code}/root-cause/{process}/{key} + P2P-only forward 'exceptions →' link ?cause=, formatCr value at risk right-aligned, formatRecurrence, named owner, target date or dimmed em-dash titled 'Not started — no commitment yet', status via causeEliminationColor; mechanism section: two Metric tiles (CAUSES ELIMINATED 11 / GROUP OPEN EXCEPTIONS 1,980) + six grid rows data-series="eliminated" [3,5,6,8,9,11] vs data-series="open-exceptions" [2,158→1,980] over the same six points with month-name period labels from new Date() at render (§7.21 relative clock, display-only), the overclaim guard 'Eliminating a cause reduces the rate at which new exceptions arrive; it does not clear the existing pool' + the defensible line 'The 2 causes eliminated this period generated 47 exceptions last period and none in this one.'; registration per rule 16 — routes.tsx ×4 (import, breadcrumb [Group → Cause elimination], NavKey + activeNavKey early return, NAV_ITEMS entry group:'EXPLAIN' right after rootCause), CommandPalette SCREEN item 'Cause elimination' meta '{eliminated} of {identified} causes eliminated', verify-drills.mjs new item 14 — 17 checks per theme (h1 / eyebrow / pitch phrase / stat strip via numeric-sibling resolution over main span elements / register 34 rows + all three statuses / elim series exact ['3','5','6','8','9','11'] / open series exact ['2,158'…'1,980'] / node-side relationship loop with comma-stripped numerics asserting elim strictly rises AND open strictly falls across the same six points — Number('2,158') is NaN so values are stripped before comparison / guard + claim sentences / breadcrumb + active nav, then palette leg 'cause elimination' → one row matching /\d+ of \d+ causes eliminated/ → click navigates) + item 13's form-row check updated 'Unassigned'→'C. Tremblay'; verify-theme.mjs ROUTES + EXPECTED_PRESENT ['statusGreen','statusAmber','accentText','borderDefault'] (route addition not named in the task's registration list — flagged per Steps 9/10 precedent); tests — new tests/cause-backlog.test.tsx with twelve tests: dataset describe seven tests (per-entity tallies toEqual + group counts, every owner in requestOwnerPool(entityCode), target dates only on in-progress rows inside today..now+182d lexicographically, eliminatedSeries pinned [3,5,6,8,9,11], currentPeriodEliminations {count 2 · generatedLastPeriod 47} with ids CB-JCP-03/CB-JRP-07, the asserted six-point relationship — elim strictly rising AND open strictly falling across the same points with the last two equal to openExceptionsPrevious()/openExceptions(), taxonomy join name.length>0 && valueAtRisk>0 for every row) and screen describe five tests (route/breadcrumb/active nav/h1/eyebrow + stat strip via numeric-sibling resolution, register 34 rows + per-row honesty loop — IN-PROGRESS rows match a formatted date else contain '—' with inProgress counter === 6, 34 back-links matching the root-cause route shape + exactly 20 P2P-only forward links, mechanism section data-series spans exact + guard/claim sentences via within(section), palette registration navigating on Enter) + service-desk.test.tsx form test updated for the named owner; judgment calls: (1) all 34 register owners come from the owning entity's §7.17 pool — 'process lead' appears only at SPEC.md line 1521 and no named process leads exist anywhere in the spec, so a named person per row is the closest reading of §8.9/§7.30's owner requirement, (2) forward links are P2P-only because the dataset carries no O2C exception list to link into — O2C rows carry the back-link only, (3) verify-theme.mjs route addition not in the task's registration list (Steps 9/10/12 precedent), (4) mechanism grid period labels are month names derived from new Date() at render time rather than 'Period 1–6' — display-only under §7.21; gates: npm test 311 passed (19 files), verify:theme 53 passed, tsc clean, verify:drills 500 passed
- [x] Step 15 (§11, §13.5) — Ask the Control Tower — extended MockAssistantProvider behind the existing AssistantProvider interface; no drawer rebuild, no real LLM: provider.ts now computes all nine §11 seed questions at answer time from the api layer (whyAmber branches to whyCapped when a cap binds — JRP's 60 → 55 fall computed as a control event: raw score moved only 60.5 → 57.9, and "no other action moves the number" is recomputed by applying every sensitivity item; liftsFastest ranks applySensitivity deltas with a capped branch where only the cap-clearing action moves it; blockedRecurring / exposureAtClose / slaWhose / dsoMonthEnd (guarded — noAnswer when the entity has no forecast) / complianceOverdue / masterDataCost / causesEliminated group-wide), every answer returns citations (drill targets: entity home, worklist, root cause, risk-control, service desk, predictive, compliance register, data quality, cause backlog + hash anchors #fct-moves / #fct-stage-COL / #fct-ic-netting) and 2–3 follow-up actions through the existing chip mechanism; an unrecognised question gets a plain no-answer ("The dataset has no answer for that question, so I will not guess.") with two seeded follow-ups — never fabricate (§11 item 5); interface extension is additive only: optional `answerMeta?(question, context): AnswerMeta | undefined` (Citation {label,to} / FollowUpAction {label,to?,ask?}) so existing providers keep compiling and the drawer falls back to its own default chips when a provider supplies none; AssistantDrawer.tsx — Message gains optional citations/followUps (backward compatible with the follow-up chip contract), committed answers render a 'Sources' row of clickable citation chips above the follow-up row, footer line "Answers resolve to transactions, owners and service records. Nothing is asserted without a source.", PRESET_QUESTIONS swapped for three §11 seeds ('Why is this entity amber?' / 'What lifts it fastest?' / 'What is our exposure at close?') — content change only, the drawer's UI contract is untouched; ask() wiring per task item 7 — EntityHome: CAPPED badge gains "Ask why it is capped" (poses the capped variant), consequence-strip header gains "Ask about this exposure", recommended-actions button now asks a dynamic question ('Why is this entity showing amber?' / 'Why is this entity capped?'), and the "What moves this score" section carries id="fct-moves" for citation anchors; RootCause: title column gains a process-branching ask button — p2p poses "Why do blocked invoices keep recurring?", o2c poses "What will DSO be at month-end?" (judgment call); GroupView's CAPPED badge deliberately not wired (minimal change — flagged); tests/palette-assistant.test.tsx — the four old AI-drawer tests replaced by five: JGL amber ask streams the computed answer ("weakest dimensions are working capital at 58") and commits with source + follow-up chips ('Open the worklist' appears twice — citation and follow-up; chip navigates to /entity/JGL/p2p/invoices, then 'Show root cause' → default pair), JRP capped ask streams "fell from 60 to 55" + the recomputed no-other-action clause + a 'Score history · 60 → 55' citation (longest seed — cursor wait extended to 8 s), top-bar toggle asserts the new presets plus the footer grounding line, preset 'What lifts it fastest?' streams JGL's computed top move "lifting the score from 74 to 81", and free-text 'How is the group doing?' gets the plain no-answer; scripts/verify-theme.mjs drawer probe updated to the new first preset ('Why is this entity amber?') — substring match over buttons, unique at '/'; judgment calls: (a) all nine §11 seeds implemented although the task said "six" — the spec's seed list has nine and item 5 requires an honest no-answer for anything else, (b) answerMeta() as an optional additive method on AssistantProvider — "do not change the interface" read as don't break ask()'s streaming contract; citations must flow somewhere and an optional method keeps every existing provider compiling, (c) the preset swap is a content change inside the unchanged drawer UI, (d) the o2c root-cause page asks the DSO month-end question rather than a recurring-invoices one — it is the o2c analogue of "why do these keep happening", (e) exposureAtClose citations reuse EntityHome's own drill URLs; gates: npm test 312 passed (19 files), verify:theme 53 passed, tsc clean, verify:drills 500 passed
- [x] Step 16 (§9.1, §8.4, §8.7, §8.10, §8.6.1, §10) — navigation, freshness and final QA — twelve items: (1) rail reorganised into the six §9.1 labelled groups — RAIL_GROUPS ['OVERVIEW','PROCESS','EXPLAIN','ASSURE','FORWARD','SERVICE'] in routes.tsx with a group field on all 14 NAV_ITEMS (counts still derive from API accessors, no literals), Rail renders items under their mono labels; counterparty pages stay drill-only with no rail entries; (2) §8.7 freshness stamp — new shared FreshnessStamp (source list + 'as of 06:00 IST', mono textFaintest) on every screen header, mixed-source screens listing all sources (EntityHome SAP ECC · close tracker · trial balance extract; RiskControl SAP ECC · AP tool control log); (3) §8.4 drill-through audit — GroupView's close-card figures tagged 'read-only · source: close tracker'; the remaining non-drillable figures (L0 header KPIs, chart bars, signal/obligation value-at-risk lines) left as reported judgment calls; (4) §8.10 cross-process trace — new CrossProcessTrace component, four-node chain missing goods receipt → blocked invoice → understated accrual → close exposure, current node a bordered span and the other three links, rendered at all four points (RootCause p2p+missing-gr 'gr', Worklist ?cause=missing-gr 'invoice', EntityHome consequence section 'accrual', GroupView close card 'close' on DEFAULT_ENTITY) so the chain is clickable from any point; (5) layout collisions fixed — WorkingCapital effort/owner merge and overlaps introduced by the new fields; (6) GroupView CAPPED badge wired to ask() via an adjacent button that poses the question against the row's entity, not the page default, matching EntityHome; (7) mode coverage walked across all three modes on every screen — GroupView close card hidden in BAU (grid drops to two columns) with a mode-aware eyebrow, no close-window figure or 'Day 4' string renders outside the close window anywhere; (8) PROJECT-MAP.md rewritten for the doubled codebase and README corrected in seven groups — score bands ≥85/65–84/<65, six-group nav with counts + drill-only note, DEMO MODE toggle + mode-derived period label, group-view KPIs (76 / ₹100.4 cr / 1,980) + CAPPED rows + BAU-hidden close card + trace strip, entity-home header + CAPPED reveal/ask, data model vetoes[] + §3.2 contractual weights disclosure, out-of-scope list; (9) all four gates green and zero literal hex in src/components|pages|app|features — tokens.ts and index.css remain the only sanctioned colour homes; (10) verify:theme's contrast SURFACES matrix extended to every screen built since Step 3b and reportContrast() made ENFORCING (violations returned and gated by check(); exemptions mirror the tokens.test.ts table, unresolved colours are violations) — the walk now reaches capped entity JRP in situ (statusRed×bgRiskSoft passes AA both themes: light 5.17 / dark 5.42), and its two findings were genuine sub-AA defects fixed in components rather than exempted: RootCause's intervention dash bullet accent → accentText, RiskControl's 'DEMO ACCESS' caption + inactive toggle label textFaint → textMuted on bgRiskSoft; paletteMatrix stays informational (worst pair dark textFaintest×bgAccentSoft 2.41 / light 2.12); (11) §8.6.1 — every derived KPI exposes its definition via the native title attribute, with the text living beside the accessor in score.ts (GROUP_SCORE_DEFINITION / VALUE_AT_RISK_DEFINITION / OPEN_EXCEPTIONS_DEFINITION on the GroupView header KPIs, AGGREGATED_ROW_DEFINITION on aggregated rows, dimension definitions on DimensionBar) so no figure can fail to answer 'what is counted here'; (12) cross-screen consistency check run and reported as a list — AP blocked ₹18.6 cr consistent everywhere for JGL; ageing buckets tie to stated totals for JGL but the P2P/O2C cockpits render the JGL-only bucket datasets on every entity with no gate; one RAG dot without an accompanying text label (TopBar entity chip); causes all in the §6.1 taxonomy (two naming notes: O2C 'Credit block delays' is spec/08 dataset vocabulary, recurringCauses 'Approval delays' vs causes.ts 'Approval pending'); five hardcoded-figure sites found and reported unfixed per the task's no-fix mandate — WorkingCapital releasable ₹4.2 cr + CommandPalette meta (per-entity field exists), CommandPalette '327 items', EntityHome issues array JGL literals where per-entity fields exist, EntityHome group-level '27 tickets' rendered per entity; judgment calls: PROCESS group renders only the two existing cockpits (no R2R cockpit screen), rail label stays 'Service desk' rather than §9.1's 'Finance Service Desk', item 6 adds a separate ask button beside the test-locked badge rather than repurposing it, item 7 hides the close card in BAU rather than rewording it, node-4 'close exposure' links to the GroupView close-card hash (hidden in BAU by design) and the GroupView trace uses DEFAULT_ENTITY via a circular-but-safe import from app/routes, item 11 touched CauseBacklog.tsx though not named (same derived figure) and chose native title as the hover mechanism; verify-drills' item6 anchor count scoped to a:not(.fct-trace-link) — the §8.10 trace inside #fct-consequence adds three links and the check is about the four consequence figures only; gates: npm test 319 passed (20 files), verify:theme 66 passed, tsc clean, verify:drills 500 passed
- [x] Step 16b (§7.31, §7.13, §7.17, §6.1, §9.1, §10) — close out the consistency findings — six fixes from the item-12 audit: (1) ageing buckets per entity per §7.31 — mock/misc.ts gains BLOCKED_AGEING_SHARES (shares of each entity's blocked AP; the profile stops at the bucket its pool oldest reaches, so array length encodes count: JCP/JHS 3 · JPS 4 · JGL/JBL/JRP 5) allocated against apBlocked.current via largest-remainder in decicr units — exact sum tie by construction and reproducing JGL's pinned ₹5.9/4.2/4.1/2.6/1.8 cr; RECEIVABLES_AGEING_CR splits per entity with the hard ties b4+b5 = arOver90.current and Σfive = O2C collection stage in-flight (§7.25) asserted for every entity in api.test.ts — the arithmetic a chartered accountant does unprompted, so JBL's cockpit can never again show ₹4.1 cr beside a chart summing ₹18.6 cr; accessors getBlockedInvoiceAgeing(code)/getReceivablesAgeing(code) take the code as required with ?? [] fallback and both cockpits pass their own; (2) §7.13 pool oldest corrected — apBlockedOldestDays 118/142/76/34/51/168 and arOver90OldestDays 148/172/112/98/104/210 (every value now exceeds 90 where the >90-day bucket is real; JPS 96→112 per the spec table though only JCP/JHS were named); §7.17 relaxed — no sample row may exceed the pool oldest but the oldest row need not equal it, so the equality assertion dropped (per-row ≤ kept) and the seeded samples stay valid since every new ceiling is ≥ the old; (3) five hardcoded JGL figures removed — WorkingCapital's releasable KPI now reads entity.metrics.releasableCash (JBL shows ₹2.6 cr, matching its own home), CommandPalette worklist/working-capital metas derive from getEntity(entityCode) with '—' fallback for unknown codes instead of borrowing JGL's 327 items / ₹4.2 cr, and the EntityHome issues array is fully data-driven (oldest days, reconAgedBreaks items, highRiskJEs JEs all per entity — the '18 items' literal no longer contradicts the panel beside it); queriesOverdue moved from ServiceControl to EntityMetrics pinned 27/34/11/4/8/41 (§7.31 — tracks the service dimension inversely), read by P2PCockpit's 'Queries overdue' row and EntityHome's tickets line; (4) §10 — the TopBar entity chip now pairs its StatusDot with the score number in matching colour, so colour is never the only carrier of meaning anywhere in the app; (5) one cause, one name — recurringCauses 'Approval delays' → 'Approval pending', matching causes.ts key approval-pending (§6.1's taxonomy reads the same everywhere); (6) §9.1 — rail label 'Service desk' → 'Finance Service Desk', extended to the breadcrumb, the palette SCREEN item and the screen's Eyebrow (the assistant follow-up chip stays a lowercase sentence); judgment calls: receivables intra-subtotal splits chosen by me (spec pins only b1–3 / b4+5 / total subtotals; all monotone non-increasing, JRP flatter to match its 'nearly flat' profile), JRP's blocked buckets land [3.0, 2.8, 3.2, 2.9, 2.8] via largest-remainder (naive rounding would sum ₹14.6 cr against the pool's ₹14.7 cr), TopBar shows the score number rather than a status word (the dot already carries colour; the number is the unambiguous adjacent carrier), and the 'drillable but not linked' inventory items stay as-is per the task — a route exists for each and the current targets are defensible; gates: npm test 321 passed (20 files), verify:theme 66 passed, tsc clean, verify:drills 500 passed
- [x] Step 17 (§3.7, §7.11, §7.13, §7.17, §7.20, §7.29, §8.5.1) — the demo script / verify the eleven-beat walk end to end and write it down — no app code changed (only DEMO.md + a scratch walk harness added), so this step proves every beat of the orals still works rather than building anything: walked all eleven beats in the running app via a self-contained headless Chrome+CDP driver (scripts/.demo-walk.tmp.mjs, debug port 9702; plumbing identical to verify-drills.mjs — chrome spawn --headless=new, waitForTarget over /json/list, Node global WebSocket, Runtime.evaluate returnByValue/awaitPromise, navigate waits loadEventFired + non-transparent body bg, clickAt via Input.dispatchMouseEvent) with per-beat probes (find-by-text for buttons/links, #fct-consequence strip links, button-only text match because group-view row <a> links shadow inner buttons in document order); final run exit=0 at 43 PASS / 2 FAIL where both FAILs are the genuine screen-vs-script discrepancies the task asks me to report (not script bugs): beat 5 — clicking ₹6.4 cr lands on /entity/JGL/p2p/invoices?cause=missing-gr whose header reads "4 of 327 shown · ₹5.47 cr of ₹18.6 cr · >30 days ₹5.00 cr · 1 of 38 resolvable in this view", while the script's stated figures (12 of 327 · ₹12.77 cr · >30 days ₹8.99 cr · 4 of 38) are exactly the UNFILTERED JGL worklist header (§7.20-pinned string); both headers state sample+pool correctly and the filtered view is internally consistent (its four rows sum to ₹5.47 cr; §7.17 requires ≥3 goods-receipt rows per entity — JGL has four), so the app matches SPEC.md and no figure is wrong, beat 8 — the bar shows JGL's own split (client 23·72% / provider 6·19% / system & third party 3·9%) with the group comparison in the footnote as the unmerged spec-pinned form "JGL 72 / 19 / 6 / 3 · group 71 / 18 / 7 / 4" (§7.11), whereas the script's "71 / 18 / 11" merges system (7) + third-party (4) = 11 — numbers consistent, grouping differs; beat-11 note — "eight SLAs given a clock start" is §7.29's pitch phrase (line 1478), not a rendered table: on screen the clock-start mechanism appears as h1 "The clock starts here." + queue RAISED ON → AGE D / STOPPED H columns (raised-on = SLA clock start per §7.21) + the three desk SLAs' live to-date figures ("measuring since 27 Aug 2026 · first full-period report from OCT-2026" — date-relative, verified on 01 Sep 2026), eight = five already-measurable + three needs-service-desk; wrote DEMO.md at repo root recording the click path, exact verified figures and the one line each beat makes for all eleven beats (group view pre-close score 76 / JRP red 55 CAPPED · badge reveal raw 57.9 fall 60→55 vs raw 60.5→57.9 · assistant "why is this entity capped?" with drillable citations · JGL top issue AP blocked ₹18.6 cr oldest 118 d + consequence strip ₹6.4 cr will-not-accrue-in-3-days · accrual drill to the filtered worklist, oldest sample row AP-104402 Kaveri Solvents ₹1.28 cr 52 d vs pool 118 d · root cause Missing GR 34% / 8.4-day lag / 5th consecutive month / 11 vendors / two plants at 72% · cause elimination 34 identified 11 eliminated + mechanism chart with rate-not-pool caveat · service & attribution gross 95.2% vs net 98.6% + greyed SLAs not yet measurable · predictive DSO 62→72 falling to 67.9 on resolving the largest driver live · risk & control restricted banner + five integrity categories "everything the auditor will find, ninety days earlier" · service desk clock start + 37% deflection), plus a Discrepancy report (beats 5 and 8 characterised above + the beat-11 clarification) and an If-they-ask section with six answers sourced from SPEC.md: why the group score is an unweighted mean (§3.7 — assurance obligations attach to each legal entity regardless of size; value-weighting §3.8 applies to exposure not assurance; mean over what is on screen so anyone can verify by averaging the rows), what "open exceptions" counts (Σ across six entities of apBlockedCount + o2cExceptionCount + reconAgedBreaks = 1,014 + 888 + 78 = 1,980 with per-entity components — items needing human action before close, not a single queue), why some SLAs have no percentage (two kinds: the three desk SLAs only got a clock start on 27 Aug 2026 so they report live to-date figures but no achievement % until the first full period OCT-2026; and the two needs-register pair stay greyed because "a service desk does not give you audit findings or a QC sample"), why DPO has no colour (§8.5.1 direction-neutral inverse:null — rising DPO is good from negotiated terms, bad from unprocessable invoices, and the platform cannot tell those apart so colouring the delta asserts something indefensible; restraint as a credibility signal), why accrual exposure is ₹6.4 cr not ₹18.6 cr (only the no-goods-receipt portion — no receipt means no accrual; for JGL exactly the missing-GR cause value §7.5, "one number, one meaning", lines 1644–1648), and why the worklist shows twelve rows against a pool of 327 with oldest sample row 52 d vs entity 118 d (the twelve are a deterministic sample of the 327-pool §7.17; wherever a sample is shown against a pool figure both are stated §7.20; the oldest sample row need not equal the pool's oldest — 52 d is the oldest of the twelve, 118 d is the pool's oldest §7.13, and the blocked-ageing chart carries a >90-day bucket which would be impossible if nothing exceeded 52 days; same distinction runs through "4 of 38 resolvable in this view"); no new screens so rule-16 registration untouched (routes/palette/verify-drills unchanged); scratch harness deleted after the step; gates: npm test 321 passed (20 files), verify:theme 66 passed, tsc clean, verify:drills 500 passed
- [x] Step 17b (§7.20) — filtered denominators, and script corrections — one code fix plus DEMO.md corrections. (1) Real defect the walk surfaced: filtered worklist denominators did not follow the filter — the goods-receipt view read "4 of 327 shown · ₹5.47 cr of ₹18.6 cr", implying four of JGL's 327 blocked invoices were missing-GR items (wrong by a factor of three). New causePool(entityCode, causeKey) in src/api: pool value = the taxonomy node's valueAtRisk (the same figure every root-cause page shows), pool count = largest-remainder over sharePct × apBlockedCount so the six pools partition the entity's blocked count exactly — JGL 111/72/59/36/26/23 sums to 327 and matches the §7.20 table; Worklist uses it when filtered to a known p2p cause and falls back to entity-wide for unknown/non-p2p causes (the ?cause=not-a-cause test stays intact); both spec properties asserted in tests/api.test.ts — pool counts sum to apBlockedCount per entity, and sampled rows never exceed a cause's value at risk (holds for all six entities; no data change needed). Affected assertions updated in-step: the filtered-header expectation in p2p-worklist-exception.test.tsx and verify-drills.mjs item 6. (2) DEMO.md corrections where the script was wrong, not the app: beat 5 now records the corrected header "4 of 111 shown · ₹5.47 cr of ₹6.4 cr …" with a note that four rows carrying most of the cause value is correct because the sample is value-ranked; beat 8 records JGL's own split merged to three categories (23/6/3) with the group beneath as "JGL 72 / 19 / 6 / 3 · group 71 / 18 / 7 / 4" — the script's "71 / 18 / 11" was the group's merged form (§7.22: an entity page shows the entity's split); beat 11 keeps "eight SLAs given a clock start" as the spoken pitch line and records what actually renders (h1 "The clock starts here.", RAISED ON column, three desk SLAs to date — eight = five already-measurable + three newly so); discrepancy report rewritten: one defect fixed this step + two script-shorthand cases. No new screens, so rule-16 registration untouched; gates: npm test 335 passed (20 files), verify:theme 66 passed, tsc clean, verify:drills 500 passed
- [x] Step 18 (§15) — The agent workforce — data model + roster screen per §15.1/§15.2/§15.5: types.ts gains Agent / Delegation / AgentMetrics / AgentAction (status 'live' | 'designed' per task, overriding §15.5's active/paused/shadow; metrics optional — only live agents carry them), new src/api/mock/agents.ts with all EIGHTEEN roles from the §15.2 table: nine live per the Built column {follow-up, master-data, commitments, approval-routing, match-resolution, duplicate-adjudication, provisioning, credit-release, cash-application}, nine designed; each carries scope, delegation of authority (mayDo / neverActsOn / requiresDualControl), a named human supervisor from that entity's §7.17 pool via round-robin ENTITY_CYCLE + pool[((n−1)%3) % pool.length (#1 P. Nair … #18 C. Tremblay), advisoryOnly on the four flag-and-nudge roles {4,5,13,14} and proposesOnly on payment proposal; per-agent caps/metrics seeded mulberry32(fnv1a(`agents:cap:${id}`)) under §0 with invariants resolved+escalated ≤ actions · overridden ≤ resolved · reversed ≤ resolved · withoutReview ≤ actedOn, six-point non-decreasing resolvedShareTrend ending at the current share %, zero valueActedOnCr for follow-up/master-data; 16-entry action log whose targets resolve to real openable records (exceptions with matching entityCode, requests in listRequests(), credit blocks on blocked customers) — commitments deliberately has none (§15.1: it acts before anything exists to act on); index.ts accessors agentWorkforceSummary() / agentRates() / agentReversibility() / coverageStrip() computed at read time (never stored), designed agents' rates return nulls not invented figures; screen src/pages/Agents.tsx at /agents — ONE screen per §15.5.1: workforce summary strip (LIVE ROLES 9 of 18 · PREVENTIVE 7 of 18 + five computed totals), the §15.2.0 lifecycle coverage strip up top (seven P2P PR→PAY + seven O2C ORD→CSH stages, agents positioned where they act, preventive/reactive coloured differently via agentTypeColor in derive.ts — accentText/statusAmber so light-theme AA holds; DLV and DSP render as visible gaps, INV carries the '(pre-close)' provisioning note, '+ 1, 2 across all stages' legend, 'Next process to cover — R2R' named rather than shown empty), boundary box stating payment proposal never releases a run + the four advisory-only agents change nothing ('the boundary a controller looks for first'), roster grouped Shared/P2P/O2C with REACTIVE before PREVENTIVE per §15.2.1 (seven of eighteen preventive — table authoritative where its prose disagrees) sortable by escalation and override rate (live never follows designed after sort), each card shown as staff: name, scope, what it may do, what bounds it, who supervises, whether actions are reversible; live cards carry actions/resolved share/escalation/override + trend Metric, designed cards greyed with 'WOULD HOLD' delegation and dashes where metrics would be (the §7.7 honest-absence pattern); clicking a card expands its record inline on the same route — full delegation, named supervisor, own action log with openable targets + precedents, performance over time; §15.8's eight never-automate items rendered prominently in their own section, not a footnote; 'simulated · demonstrates the operating model, not a live agent' per §15.1 on the page header and each expanded record (not all 18 cards — flagged); registration per rule 6 — routes.tsx ×4 under a new AGENTS rail group appended last after SERVICE (nav count derived as the live-role count so the rail never claims what isn't built), CommandPalette SCREEN item 'Agents' meta '{live} of {total} roles live' via agentWorkforceSummary(), verify-drills.mjs new item 15 (h1/eyebrow/simulated label/cycle line 'agents last ran 06:42 · next cycle 07:00'/summary stats scoped to [data-fct-summary]/roster 18 cards + 9 live badges/coverage strip 14 stages + PO three chips + DLV/DSP zero/boundary sentence/eight never-items/palette leg), verify-theme.mjs ROUTES ['agents'] + EXPECTED_PRESENT ['accentText','statusAmber','bgRiskSoft','bgWarnSoft','borderDefault']; tests — new tests/agents.test.tsx with 23 tests (12 dataset: eighteen roles pinned, seven preventive set, advisory-only + payment-proposal boundary, supervisors ∈ §7.17 pools, live-metrics/designed-absence split, metric invariants + trend tail equality, zero-value agents, action-log existence incl. commitments' honest absence, every log target + precedent resolves to a real record, most-recent-first ordering, coverage-strip shape with DLV/DSP gaps and billing-readiness at ORD+BIL, workforce summary recomputed independently; 11 screen: route/breadcrumb/active nav/headline stats, roster 18 cards each stating live or designed, default group/type order pinned exactly, escalation sort keeps live before designed in every section, coverage strip on screen with R2R named, boundary box, payment-proposal WOULD HOLD + advisory 'changes nothing', §15.8 eight items verbatim, follow-up record expansion with 3 actions + openable invoice link + simulated label, commitments/designed honest absences, palette registration) + two routes.test.ts cases (breadcrumb [Group → Agents] + activeNavKey 'agents'); judgment calls: (i) status type follows the task's 'live'|'designed' over §15.5's lifecycle words — the honesty split is the point, (ii) live set read from §15.2's Built column, (iii) preventive set {3,4,5,6,13,14,15} follows the table + 'seven of eighteen' where §15.2.1's prose names a different six-plus-two, (iv) supervisor round-robin formula is invented — the spec names no per-agent supervisors, only pools, (v) metrics/caps seeded under §0 with invariants asserted rather than pinned values, (vi) commitments has no action log by design (§15.1) and its record says why; designed agents' records say 'not built', (vii) action-log entries reference exceptions that remain open in the worklist — the prototype has no write-back, so the log is a period record of decisions, not live state sync, (viii) record = inline expansion on the single /agents route rather than a second route, (ix) sort via local state not URL params, (x) SimTag on header + records only, not all 18 cards ('every agent surface' read as each distinct surface), (xi) palette query 'agents' also matches the seeded vendor 'Aravalli Reagents', so both the unit test and verify-drills assert on the screen row's meta /\d+ of \d+ roles live/ rather than total row count, and the unit test clicks the picked row instead of pressing Enter; gates: npm test 360 passed (21 files), verify:theme 68 passed, tsc clean, verify:drills 574 passed
- [x] Step 18b (§10.1) — take the rationale off the screen — no data or logic changes; every on-screen sentence that explains, persuades or asserts is deleted or reduced to what a product a company had paid for would say, per §10.1's three tiers (rationale and assertion never on screen; reference data stays as fields): /agents per the §10.1 replacement table — intro line about honesty/credibility ('simulated · demonstrates the operating model, not a live agent') deleted, replaced by SimTag 'Simulated data' (§15.1) in the header and each expanded record; summary reads '18 roles · 9 active'; boundary section 'THE BOUNDARY A CONTROLLER LOOKS FOR FIRST' deleted entirely (item 3 supersedes item 1's rename to 'AUTHORITY LIMITS') with the same information moved onto each agent's own record as a delegation field — AUTHORITY 'proposes only' on payment-proposal, 'advisory' on the four advisory agents (buying-compliance · receipt-discipline · credit-watch · billing-readiness), rendered from the existing proposesOnly/advisoryOnly flags with no new stored data; stage-count caption 'seven P2P and seven O2C stages · agents positioned where they act' deleted (the strip already shows it); R2R line becomes 'Record to report — in the roadmap'; status badges 'live'/'designed' → 'Active'/'Not active' (§15.2) with stat label LIVE ROLES → ACTIVE ROLES; cycle line 'agents last ran 06:42 · next cycle 07:00' kept (§15.1.1); other screens — RiskControl h1 'Everything the auditor will find, ninety days earlier.' → 'Risk & control' (eyebrow removed) + no-duplication paragraph deleted; ServiceDesk h1 'The clock starts here.' → 'Finance Service Desk' + pitch line 'a single structured intake for every request into the service — …' deleted, measuring-since honesty line kept; CauseBacklog h1 'Continuous improvement, evidenced.' → 'Cause elimination' + intro sentence deleted + register header reduced to '{n} causes' (dropping 'owners are named people, not departments'), overclaim guard and p6 claim kept; ServiceAttribution h1 'Where the delays come from' → 'Service & attribution' + gross/net sentence 'We met 95.2% against the contract; 98.6% of what we control.' deleted + 'Service credits attach here, and only here' reduced to 'Service credits attach here'; WorkingCapital h1 'Cash locked in exceptions' → 'Working capital'; O2C cockpit h1 'Revenue to cash, as one flow' → 'Order to cash'; P2P cockpit h1 'End-to-end flow, not seven separate reports' → 'Procure to pay' + cost-centre footer reduced to 'Open POs not yet invoiced' (dropping 'the part most tools miss'); RootCause h1 (both processes) → flat 'Root cause', hierarchy eyebrow kept; CompliancePage h1 'Every statutory obligation, in the jurisdiction that owns it.' → 'Compliance'; DataQualityPage h1 'Where master data fails — and the working capital it becomes.' → 'Data quality'; CommandPalette Agents meta '{live} of {total} roles live' → 'roles active'; AssistantDrawer's two assertion paragraphs deleted ('Grounded on the finance semantic model…' + footer 'Answers resolve to transactions, owners and service records. Nothing is asserted without a source.'); what stays per §10.1 3b — figure-meaning labels ('Open work in progress', '12 of 327 shown', 'read-only · source: close tracker'), the RESTRICTED strip + DEMO toggle (§15.1.1), ServiceDesk's 'demo · writes to local state only' form tag, CauseBacklog's overclaim guard + defensible p6 sentence; tests — all thirteen affected files updated in-step (flat h1s via heading role, eyebrow previousElementSibling assertions dropped where the eyebrow was deleted, risk-control no-duplication test deleted outright 360→359, agents.test.tsx boundary-box tests replaced by per-record AUTHORITY field assertions); scripts — verify-theme.mjs EXPECTED_PRESENT.agents drops 'bgWarnSoft' (the tinted boundary box is gone; the list must track what renders) + comment updated; verify-drills.mjs item 15 rewritten off the deleted copy (h1/eyebrow/simulated-label/boundary-sentence/stage-caption checks replaced by flat-title + SimTag + a per-record AUTHORITY leg — split click → 200 ms flush → read over payment-proposal + the four advisory agents, because the roster holds one expandedId at a time so each record must be read before the next card opens; AG_SECTIONS_JS badge lookup 'live'/'designed' → 'Active'/'Not active', which also repairs the escalation-sort ordering check that had gone vacuous after the rename) + items 7–14 h1/pitch-phrase checks updated to the flat titles (574→562 executed checks, −12 = deleted-copy assertions); DEMO.md — three on-screen string quotes corrected where they no longer render (beat 8 gross/net sentence removed; beat 10 h1 'Risk & control'; beat 11 h1 'Finance Service Desk'), presenter's spoken '**The line:**' narrative left as-is; judgment calls: (a) item 3 supersedes item 1 — the §10.1 table says delete the boundary panel, so no 'AUTHORITY LIMITS' heading exists and the delegation rides on each record instead, (b) task string 'Record to report — in the roadmap' overrides §10.1's 'not yet covered', (c) h1 flattening pattern A (eyebrow == canonical name + tagline h1 → flat h1, eyebrow deleted: RiskControl/ServiceDesk/CauseBacklog/Compliance/DataQuality/ServiceAttribution/WorkingCapital) vs pattern B (hierarchy eyebrow kept, only h1 flattened: P2P/O2C cockpits, RootCause), (d) CauseBacklog's overclaim guard + p6 claim and ServiceDesk's measuring-since line kept — they state what the screen does or measures, not a pitch, and drills still assert them, (e) 'The mechanism' eyebrow on CauseBacklog kept as a section label, (f) DEMO.md treated as a living walk doc — only exact rendered-string quotes fixed, spoken lines untouched, (g) README.md left unedited — it documents the original design-reference handoff, not current screen copy; partial updates would mislead, (h) AG_SECTIONS_JS badge rename also repairs a vacuous sort-order check, (i) verify-theme's agents expectation list is evidence-from-source-grep and must track the rendered screen, so bgWarnSoft drops with the boundary box, (j) 574→562 drill-check drop is exactly the deleted-copy assertions (item 7 attribution sentence · item 8 no-duplication note · item 13 §7.29 pitch phrase · item 14 eyebrow + §7.30 pitch phrase, plus four old h1 checks replaced by flat-title checks); gates: npm test 359 passed (21 files), verify:theme 68 passed, tsc clean, verify:drills 562 passed
- [x] Step 18c (§10.1) — restore the descriptive titles — no data or logic changes; four screens get back the h1 that says what the screen shows or answers (product copy, not a pitch), everything else stays flat per the DESCRIBE-or-PERSUADE test (a heading may be interesting; it may not be an argument): RootCause.tsx h1 process-branching — 'Why blocked invoices keep recurring' (p2p) / 'Why receivables keep ageing' (o2c), hierarchy eyebrow 'Level 5 — Root cause' kept as in 18b pattern B; WorkingCapital.tsx → 'Cash locked in exceptions'; ServiceAttribution.tsx → 'Where the delays come from'; O2CCockpit.tsx → 'Revenue to cash, as one flow' (eyebrow 'Level 2 — Process · Order to Cash' kept); left flat per item 2: Risk & control, Finance Service Desk, Cause elimination, Compliance, Data quality, Agents and the P2P cockpit ('Procure to pay' is the honest title once 'not seven separate reports' is gone); eyebrows deleted in 18b on WorkingCapital/ServiceAttribution NOT re-added — h1 text only, no other changes; tests updated in-step: rootcause-workingcapital.test.tsx ×4 (p2p + o2c heading assertions, working-capital heading, and the dead line-186 `getByText(/working capital/i)` deleted — it targeted the eyebrow 18b removed and had kept passing only by matching the flat h1), app-shell.test.tsx ×2 (JGL rail root-cause + working-capital headings; breadcrumb/nav toContain assertions untouched), drill-paths.test.tsx ×4 (default p2p pair, o2c loop, both-processes loop now process-conditional on the heading name, and the o2c-starting path via main()), o2c-cockpit.test.tsx ×2 (replace_all 'Order to cash' → restored title; eyebrow check untouched), p2p-worklist-exception.test.tsx ×1 ('Why does this keep happening?' landing), service-attribution.test.tsx ×1 (breadcrumb/nav/dimension-row assertions kept); scripts — verify-drills.mjs P2P_TITLE/O2C_TITLE constants + the svc.h1 check updated to the restored strings, no checks added or removed so 562 stands; DEMO.md untouched — repo-wide grep confirmed it quotes none of the five restored h1s (beat 9's quoted h1 is the predictive page, out of scope); nav labels, breadcrumbs and CommandPalette meta stay flat ('Root cause', 'Working capital', …) — only screen h1s changed, so routes.test.ts label arrays and pages-group-entity dimension-label assertions are untouched; judgment calls: (a) task title says four headings but item 1 lists five strings — Root cause is one screen with two process variants, all five restored, (b) h1 text only — the eyebrows 18b deleted on WorkingCapital/ServiceAttribution stay deleted per 'No other changes', (c) dead assertion `getByText(/working capital/i)` at rootcause-workingcapital.test.tsx:186 deleted rather than re-adding the eyebrow it originally targeted, (d) drill-paths both-processes loop uses a process conditional for the heading name instead of two separate loops, (e) DEMO.md verified untouched by grep rather than edited, (f) flat labels kept everywhere outside the screen h1 — the test applies to headings that argue, and 'Root cause' in a nav rail describes; gates: npm test 359 passed (21 files), verify:theme 68 passed, tsc clean, verify-drills 562 passed
- [x] Step 18d (§10.1) — restore the two eyebrows, then sweep for tests that pass by accident — one consistency fix plus a test-quality audit; no data or logic changes: (1) Eyebrows restored on the two screens where 18b's flattening left a descriptive h1 with nothing above it, restoring the product-wide pattern eyebrow = canonical name / h1 = description — WorkingCapital.tsx gets `<Eyebrow>Working capital</Eyebrow>` back above 'Cash locked in exceptions', ServiceAttribution.tsx gets `<Eyebrow>Service & attribution</Eyebrow>` back above 'Where the delays come from'; both unknown-entity fallback branches already carried the same eyebrow (18b touched only the main branch) and were left alone; Root cause and both cockpits kept theirs, and screens whose h1 is itself the canonical name (Risk & control, Compliance, Agents, …) correctly have no eyebrow — left as-is; git diff confirms WorkingCapital.tsx ends up byte-identical to the committed file and ServiceAttribution.tsx retains only 18b's two copy deletions. (2) Sweep for assertions that pass by accident across all 21 test files: zero findings, nothing tightened or deleted — a test that passes for the wrong reason reports coverage that does not exist, so each candidate had to prove it targets what it claims. The three unit files under src assert pure functions against pinned inputs with no DOM queries (the shape cannot apply); the 18 screen/integration suites in tests/ were reviewed for the three shapes — loose regex/getByText that could match more than one element, assertions whose target no longer exists and is passing off something else, checks that would pass on an empty or default render. Every bare exact-string value was closed by a two-part argument: (i) a dataset pin in api.test.ts or mock source proves the claimed location renders exactly that string; (ii) the test currently passes and exact-string getByText throws on multiple matches, so no other element carries the identical string. Candidates reviewed: '349' / '19% exception' (stages.ts GR row + StageFlow's standalone cells — unique across JGL's seven p2p stages), group view '76' / '₹100.4 cr' / '1,980' (GroupView Kpi cells + getGroupSummary pins), '26%' — the one real gap (could be a cause share or an entity-row closePct cell) resolved via misc.ts recurringCauses[0].sharePct = 26 → it is the Missing GR causes-card value, '71%' / '54%' (closeProgress.pct / touchlessInvoicesPct pins), JGL home '78%' and JBL home '67' / '61%' (big-score span + Close tile, pinned); judgment calls: (a) Agents.tsx actually carries `<Eyebrow>Agent workforce</Eyebrow>` above h1 'Agents', contradicting the task's parenthetical that "Agents and the rest" have none — flagged here, left untouched per "leave them", (b) verified-unique exact-string bare values deliberately left unscoped — scoping would be churn without changing meaning since exact-string getByText cannot multi-match and both verification parts hold, (c) zero test-file edits needed in this step; the two dead assertions cited by the task were already handled where they surfaced (the vacuous sort-order check repaired in 18b via the badge rename, the `getByText(/working capital/i)` line deleted in 18c), (d) no new coverage added — the point was to make the existing 359 mean what they say; gates: npm test 359 passed (21 files), verify:theme 68 passed, tsc clean, verify-drills 562 passed

## 2026-09-06 — Step 19 · The agent lane
Seeded 71 AgentActions across all six entities against the seven live P2P agents (outcomes weighted to §15.4 shares), and gave the worklist an agent lane with four states, a needs-you default view, and a one-shot demo cycle (86 → 79). Exception detail now carries the full decision record (§15.1.2) with step-through and override; root cause shows each P2P cause's agent-resolvable share. Gates: test 362/362 · theme 68/0 · drills 562/0 · build clean.

## 2026-09-06 — Step 20: Touch economics
Built /touch-economics per §15.3/§15.4: six-entity funnel (JGL 54/46/32.2/13.8, 460→138 touches per thousand), JGL's two-lever glide path as three stages, and the p2p cause mix with resolvable shares; wired routes, palette, both verify scripts. Reconciliation asserted in tests and drills: agent-resolved + human = manual, touches = share × 10, Σ(share×resolvable) ≈ JGL's 32.2. Gates: test 375/375 (22 files), theme 70/70, drills 600/600, build clean.
2026-09-06 — Step 21: Control of the agent. Risk & control gains an Agent governance slice (delegation breaches, reversals, overrides, value acted on without human review; each row drills into the agent's record) with rate interpretations per §15.6. New /agents/:agentId record page reachable from every roster card and the palette; Touch Economics now labels the two end states apart (§15.3). Gates: 384 tests · build clean · theme 72 checks · drills 630 checks.

2026-09-06 - Step 21b - Swept verify-drills.mjs and verify-theme.mjs for checks that pass vacuously. Eight breadcrumb probes in verify-drills matched the rail, which lists every screen label; each is now scoped to nav[aria-label="Breadcrumb"]. The per-route color-presence census in verify-theme was document-wide, so chrome-carried tokens satisfied page assertions; it is now scoped to main, and working-capital's statusAmber - present only in the top-bar score chip - dropped from its expected list. Gates: npm test 384 passed, build clean, verify:theme 72 passed, verify:drills 630 passed.

## 2026-09-06 · Step 22 — One exception, end to end
Added the missing-GR walkthrough on AP-104402 (eight steps, stepped by hand) and both credit-block decision records (Deccan released / Pasir escalated); beats 12–13 in DEMO.md. Item 0 closed: six always-passing milestone checks removed and a minimum-count guard added to FILL_AUDIT — the count ran 630→624→630, but every check can now fail; Step 22 adds 22 drill checks. Gates: tests 387/387 · build clean · theme 72/72 · drills 652/652.

2026-09-07 — Step 23, The preventive agent. Reworded six outcome strings so no agent record claims a SAP state change beside a live badge. Added the commitments data layer (open POs reconciling to the §7.4 pool), the Commitments watch on the P2P PO stage, and the PO detail page with the agent–owner exchange — confirmed slips amend date-only; ambiguous replies propose and escalate. DEMO.md beats 14/14b. Gates: test 405, theme 76, drills 698, build clean.
